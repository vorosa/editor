//======================================================================
//
// File: DerivedAssociationR_COMPPropertySource.java
//
// WARNING:      Do not edit this generated file
// Generated by: arc/BuildPropertySource.arc
// Version:      $Revision: 1.66 $
//
// (c) Copyright 2006-2014 by Mentor Graphics Corp.  All rights reserved.
//
//======================================================================

package com.mentor.nucleus.bp.ui.properties;
import org.eclipse.ui.views.properties.*;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.*;
import com.mentor.nucleus.bp.core.sorter.MetadataSortingManager;
import com.mentor.nucleus.bp.core.ui.cells.editors.*;
import com.mentor.nucleus.bp.core.util.DimensionsUtil;

public class DerivedAssociationR_COMPPropertySource implements IPropertySource {
	private DerivedAssociation_c m_inst;
	private ClassAsDerivedOneSide_c[] m_DerivedOneEnd;
	private ClassAsDerivedOtherSide_c[] m_DerivedOtherEnd;
	private PropertyDescriptor[] m_propertyDescriptors;

	MetadataSortingManager sorter = MetadataSortingManager.createDefault();

	public DerivedAssociationR_COMPPropertySource(DerivedAssociation_c inst) {
		m_inst = inst;
	}
	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getEditableValue()
	 */
	public Object getEditableValue() {
		return this;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#isPropertySet(java.lang.Object)
	 */
	public boolean isPropertySet(Object id) {
		return false;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#resetPropertyValue(java.lang.Object)
	 */
	public void resetPropertyValue(Object id) {
		// do nothing
	}

	public String toString() {
		return m_inst.Get_name();
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyDescriptors()
	 */
	public IPropertyDescriptor[] getPropertyDescriptors() {
		if (m_inst == null || m_inst.isOrphaned())
			return new IPropertyDescriptor[0];
		int num_children = 0;
		ClassAsDerivedOneSide_c[] v_derivedoneend = ClassAsDerivedOneSide_c
				.getManyR_CONEsOnR214((DerivedAssociation_c) m_inst);

		m_DerivedOneEnd = v_derivedoneend;
		num_children += m_DerivedOneEnd.length;

		sorter.sort(v_derivedoneend);
		ClassAsDerivedOtherSide_c[] v_derivedotherend = ClassAsDerivedOtherSide_c
				.getManyR_COTHsOnR215((DerivedAssociation_c) m_inst);

		m_DerivedOtherEnd = v_derivedotherend;
		num_children += m_DerivedOtherEnd.length;

		sorter.sort(v_derivedotherend);
		if (m_propertyDescriptors == null
				|| 1 + num_children != m_propertyDescriptors.length) {
			m_propertyDescriptors = new PropertyDescriptor[1 + num_children];
			boolean readonly = false;

			m_propertyDescriptors[0] = new TextPropertyDescriptor("Rel_Chn",
					"Association Chain");
			m_propertyDescriptors[0]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);

			int d_index = 1;
			for (int i = 0; i < m_DerivedOneEnd.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"DerivedOneEnd." + String.valueOf(i),
						"Class As Derived One Side");
				m_propertyDescriptors[d_index].setCategory("Derived One End");
				++d_index;
			}

			for (int i = 0; i < m_DerivedOtherEnd.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"DerivedOtherEnd." + String.valueOf(i),
						"Class As Derived Other Side");
				m_propertyDescriptors[d_index].setCategory("Derived Other End");
				++d_index;
			}

		}
		return m_propertyDescriptors;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyValue(java.lang.Object)
	 */
	public Object getPropertyValue(Object id) {
		if (m_inst.isOrphaned()) {
			return "";
		}
		if (id.equals("Rel_Chn")) {
			return m_inst.getRel_chn();
		}
		int d_index = 1;
		for (int i = 0; i < m_DerivedOneEnd.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new DerivedOneEndR_CONEPropertySource(m_DerivedOneEnd[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_DerivedOtherEnd.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new DerivedOtherEndR_COTHPropertySource(
						m_DerivedOtherEnd[i]);
			}
			++d_index;
		}

		return null;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#setPropertyValue(java.lang.Object, java.lang.Object)
	 */
	public void setPropertyValue(Object id, Object value) {
		boolean value_changed = false;
		String trans_name = "";
		Transaction tr = null;
		Ooaofooa modelRoot = (Ooaofooa) m_inst.getModelRoot();
		TransactionManager tm = TransactionManager.getSingleton();
		try {
			if (id.equals("Rel_Chn")) {
				if (!value.toString().equals(m_inst.getRel_chn())) {
					trans_name = "Change in property: 'Rel_Chn' of Derived Association";//$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setRel_chn(value.toString());
				}
			}

			// catch all exceptions and cancel the transaction
		} catch (Exception e) {
			// this can be null if there was
			// an exception starting the
			// transaction
			if (tr != null) {
				tm.cancelTransaction(tr, e);
				tr = null;
			}
			CorePlugin.logError("Transaction: " + trans_name
					+ " could not complete", e);
		}
		if (tr != null) {
			if (value_changed)
				tm.endTransaction(tr);
			else
				tm.cancelTransaction(tr);
		}
	}
}
