//======================================================================
//
// File: StateEventMatrixEntrySM_SEMEPropertySource.java
//
// WARNING:      Do not edit this generated file
// Generated by: arc/BuildPropertySource.arc
// Version:      $Revision: 1.66 $
//
// (c) Copyright 2006-2014 by Mentor Graphics Corp.  All rights reserved.
//
//======================================================================

package com.mentor.nucleus.bp.ui.properties;
import org.eclipse.ui.views.properties.*;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.*;
import com.mentor.nucleus.bp.core.sorter.MetadataSortingManager;
import com.mentor.nucleus.bp.core.ui.cells.editors.*;
import com.mentor.nucleus.bp.core.util.DimensionsUtil;

public class StateEventMatrixEntrySM_SEMEPropertySource
		implements
			IPropertySource {
	private StateEventMatrixEntry_c m_inst;
	private EventIgnored_c[] m_EventIgnored;
	private CantHappen_c[] m_EventCantHappen;
	private PropertyDescriptor[] m_propertyDescriptors;

	MetadataSortingManager sorter = MetadataSortingManager.createDefault();

	public StateEventMatrixEntrySM_SEMEPropertySource(
			StateEventMatrixEntry_c inst) {
		m_inst = inst;
	}
	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getEditableValue()
	 */
	public Object getEditableValue() {
		return this;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#isPropertySet(java.lang.Object)
	 */
	public boolean isPropertySet(Object id) {
		return false;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#resetPropertyValue(java.lang.Object)
	 */
	public void resetPropertyValue(Object id) {
		// do nothing
	}

	public String toString() {
		return m_inst.Get_name();
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyDescriptors()
	 */
	public IPropertyDescriptor[] getPropertyDescriptors() {
		if (m_inst == null || m_inst.isOrphaned())
			return new IPropertyDescriptor[0];
		int num_children = 0;
		EventIgnored_c[] v_eventignored = EventIgnored_c
				.getManySM_EIGNsOnR504((StateEventMatrixEntry_c) m_inst);

		m_EventIgnored = v_eventignored;
		num_children += m_EventIgnored.length;

		sorter.sort(v_eventignored);
		CantHappen_c[] v_eventcanthappen = CantHappen_c
				.getManySM_CHsOnR504((StateEventMatrixEntry_c) m_inst);

		m_EventCantHappen = v_eventcanthappen;
		num_children += m_EventCantHappen.length;

		sorter.sort(v_eventcanthappen);
		if (m_propertyDescriptors == null
				|| 0 + num_children != m_propertyDescriptors.length) {
			m_propertyDescriptors = new PropertyDescriptor[0 + num_children];
			boolean readonly = false;

			int d_index = 0;
			for (int i = 0; i < m_EventIgnored.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"EventIgnored." + String.valueOf(i), "Event Ignored");
				m_propertyDescriptors[d_index].setCategory("Event Ignored");
				++d_index;
			}

			for (int i = 0; i < m_EventCantHappen.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"EventCantHappen." + String.valueOf(i), "Cant Happen");
				m_propertyDescriptors[d_index].setCategory("Event Cant Happen");
				++d_index;
			}

		}
		return m_propertyDescriptors;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyValue(java.lang.Object)
	 */
	public Object getPropertyValue(Object id) {
		if (m_inst.isOrphaned()) {
			return "";
		}
		int d_index = 0;
		for (int i = 0; i < m_EventIgnored.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new EventIgnoredSM_EIGNPropertySource(m_EventIgnored[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_EventCantHappen.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new EventCantHappenSM_CHPropertySource(
						m_EventCantHappen[i]);
			}
			++d_index;
		}

		return null;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#setPropertyValue(java.lang.Object, java.lang.Object)
	 */
	public void setPropertyValue(Object id, Object value) {
	}
}
