//======================================================================
//
// File: ComponentReferencesCL_ICPropertySource.java
//
// WARNING:      Do not edit this generated file
// Generated by: arc/BuildPropertySource.arc
// Version:      $Revision: 1.66 $
//
// (c) Copyright 2006-2014 by Mentor Graphics Corp.  All rights reserved.
//
//======================================================================

package com.mentor.nucleus.bp.ui.properties;
import org.eclipse.ui.views.properties.*;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.*;
import com.mentor.nucleus.bp.core.sorter.MetadataSortingManager;
import com.mentor.nucleus.bp.core.ui.cells.editors.*;
import com.mentor.nucleus.bp.core.util.DimensionsUtil;

public class ComponentReferencesCL_ICPropertySource implements IPropertySource {
	private ComponentReference_c m_inst;
	private Component_c[] m_ComponentsReferencedComponent;
	private ImportedProvision_c[] m_ImportedProvidedInterfaces;
	private ImportedRequirement_c[] m_ImportedRequiredInterfaces;
	private PropertyDescriptor[] m_propertyDescriptors;
	private String[] Mult_vals = {"One", "Many"};
	private String[] validMult_vals() {
		String[] onlyOne = {"One"};
		if (m_inst.Manymultallowed()) {
			return Mult_vals;
		}
		return onlyOne;
	}

	MetadataSortingManager sorter = MetadataSortingManager.createDefault();

	public ComponentReferencesCL_ICPropertySource(ComponentReference_c inst) {
		m_inst = inst;
	}
	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getEditableValue()
	 */
	public Object getEditableValue() {
		return this;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#isPropertySet(java.lang.Object)
	 */
	public boolean isPropertySet(Object id) {
		return false;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#resetPropertyValue(java.lang.Object)
	 */
	public void resetPropertyValue(Object id) {
		// do nothing
	}

	public String toString() {
		return m_inst.getName();
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyDescriptors()
	 */
	public IPropertyDescriptor[] getPropertyDescriptors() {
		if (m_inst == null || m_inst.isOrphaned())
			return new IPropertyDescriptor[0];
		int num_children = 0;
		Component_c[] v_componentsReferencedComponent = Component_c
				.getManyC_CsOnR4201((ComponentReference_c) m_inst);

		m_ComponentsReferencedComponent = v_componentsReferencedComponent;
		num_children += m_ComponentsReferencedComponent.length;

		sorter.sort(v_componentsReferencedComponent);
		ImportedProvision_c[] v_importedprovidedinterfaces = ImportedProvision_c
				.getManyCL_IPsOnR4703(ImportedReference_c
						.getManyCL_IIRsOnR4700((ComponentReference_c) m_inst));

		m_ImportedProvidedInterfaces = v_importedprovidedinterfaces;
		num_children += m_ImportedProvidedInterfaces.length;

		sorter.sort(v_importedprovidedinterfaces);
		ImportedRequirement_c[] v_importedrequiredinterfaces = ImportedRequirement_c
				.getManyCL_IRsOnR4703(ImportedReference_c
						.getManyCL_IIRsOnR4700((ComponentReference_c) m_inst));

		m_ImportedRequiredInterfaces = v_importedrequiredinterfaces;
		num_children += m_ImportedRequiredInterfaces.length;

		sorter.sort(v_importedrequiredinterfaces);
		if (m_propertyDescriptors == null
				|| 3 + num_children != m_propertyDescriptors.length) {
			m_propertyDescriptors = new PropertyDescriptor[3 + num_children];
			boolean readonly = false;

			m_propertyDescriptors[0] = new EnumPropertyDescriptor("Mult",
					"Multiplicity", validMult_vals(), readonly);
			m_propertyDescriptors[0]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);
			m_propertyDescriptors[1] = new TextPropertyDescriptor(
					"ClassifierName", "Classifier Name");
			m_propertyDescriptors[1]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);
			m_propertyDescriptors[2] = new DescriptionPropertyDescriptor(
					"Descrip", "Imported Component Description", m_inst);
			m_propertyDescriptors[2]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);

			int d_index = 3;
			for (int i = 0; i < m_ComponentsReferencedComponent.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"ComponentsReferencedComponent." + String.valueOf(i),
						"Referenced Component");
				m_propertyDescriptors[d_index].setCategory("Components");
				++d_index;
			}

			for (int i = 0; i < m_ImportedProvidedInterfaces.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"ImportedProvidedInterfaces." + String.valueOf(i),
						"Imported Provision");
				m_propertyDescriptors[d_index]
						.setCategory("Imported Provided Interfaces");
				++d_index;
			}

			for (int i = 0; i < m_ImportedRequiredInterfaces.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"ImportedRequiredInterfaces." + String.valueOf(i),
						"Imported Requirement");
				m_propertyDescriptors[d_index]
						.setCategory("Imported Required Interfaces");
				++d_index;
			}

		}
		return m_propertyDescriptors;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyValue(java.lang.Object)
	 */
	public Object getPropertyValue(Object id) {
		if (m_inst.isOrphaned()) {
			return "";
		}
		if (id.equals("Mult")) {
			return Mult_vals[m_inst.getMult()];
		} else if (id.equals("ClassifierName")) {
			return m_inst.getClassifiername();
		} else if (id.equals("Descrip")) {
			return m_inst.getDescrip().replace('\n', '/');
		}
		int d_index = 3;
		for (int i = 0; i < m_ComponentsReferencedComponent.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new ComponentsC_CPropertySource(
						m_ComponentsReferencedComponent[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_ImportedProvidedInterfaces.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new ImportedProvidedInterfacesCL_IPPropertySource(
						m_ImportedProvidedInterfaces[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_ImportedRequiredInterfaces.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new ImportedRequiredInterfacesCL_IRPropertySource(
						m_ImportedRequiredInterfaces[i]);
			}
			++d_index;
		}

		return null;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#setPropertyValue(java.lang.Object, java.lang.Object)
	 */
	public void setPropertyValue(Object id, Object value) {
		boolean value_changed = false;
		String trans_name = "";
		Transaction tr = null;
		Ooaofooa modelRoot = (Ooaofooa) m_inst.getModelRoot();
		TransactionManager tm = TransactionManager.getSingleton();
		try {
			if (id.equals("Mult")) {
				int new_val = Integer.parseInt(value.toString());
				if (new_val != m_inst.getMult()) {
					trans_name = "Change in property: 'Mult' of Component Reference";//$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setMult(new_val);
				}
			} else if (id.equals("ClassifierName")) {
				if (!value.toString().equals(m_inst.getClassifiername())) {
					trans_name = "Change in property: 'ClassifierName' of Component Reference";//$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setClassifiername(value.toString());
				}
			} else if (id.equals("Descrip")) {
				if (!value.toString().equals(m_inst.getDescrip())) {
					trans_name = "Change in property: 'Descrip' of Component Reference";//$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setDescrip(value.toString());
				}
			}

			// catch all exceptions and cancel the transaction
		} catch (Exception e) {
			// this can be null if there was
			// an exception starting the
			// transaction
			if (tr != null) {
				tm.cancelTransaction(tr, e);
				tr = null;
			}
			CorePlugin.logError("Transaction: " + trans_name
					+ " could not complete", e);
		}
		if (tr != null) {
			if (value_changed)
				tm.endTransaction(tr);
			else
				tm.cancelTransaction(tr);
		}
	}
}
