//======================================================================
//
// File: ReferenceO_REFPropertySource.java
//
// WARNING:      Do not edit this generated file
// Generated by: arc/BuildPropertySource.arc
// Version:      $Revision: 1.66 $
//
// (c) Copyright 2006-2014 by Mentor Graphics Corp.  All rights reserved.
//
//======================================================================

package com.mentor.nucleus.bp.ui.properties;
import org.eclipse.ui.views.properties.*;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.*;
import com.mentor.nucleus.bp.core.sorter.MetadataSortingManager;
import com.mentor.nucleus.bp.core.ui.cells.editors.*;
import com.mentor.nucleus.bp.core.util.DimensionsUtil;

public class ReferenceO_REFPropertySource implements IPropertySource {
	private AttributeReferenceInClass_c m_inst;
	private ModelClass_c[] m_ClassReferredToClass;
	private Attribute_c[] m_AttributeReferredToAttribute;
	private Association_c[] m_AssociationReferencedOver;
	private PropertyDescriptor[] m_propertyDescriptors;

	MetadataSortingManager sorter = MetadataSortingManager.createDefault();

	public ReferenceO_REFPropertySource(AttributeReferenceInClass_c inst) {
		m_inst = inst;
	}
	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getEditableValue()
	 */
	public Object getEditableValue() {
		return this;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#isPropertySet(java.lang.Object)
	 */
	public boolean isPropertySet(Object id) {
		return false;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#resetPropertyValue(java.lang.Object)
	 */
	public void resetPropertyValue(Object id) {
		// do nothing
	}

	public String toString() {
		return m_inst.Get_name();
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyDescriptors()
	 */
	public IPropertyDescriptor[] getPropertyDescriptors() {
		if (m_inst == null || m_inst.isOrphaned())
			return new IPropertyDescriptor[0];
		int num_children = 0;
		ModelClass_c[] v_classReferredToClass = ModelClass_c
				.getManyO_OBJsOnR201(ClassInAssociation_c.getManyR_OIRsOnR203(ReferredToClassInAssoc_c.getManyR_RTOsOnR110(ReferredToIdentifierAttribute_c
						.getManyO_RTIDAsOnR111((AttributeReferenceInClass_c) m_inst))));

		m_ClassReferredToClass = v_classReferredToClass;
		num_children += m_ClassReferredToClass.length;

		sorter.sort(v_classReferredToClass);
		Attribute_c[] v_attributeReferredToAttribute = Attribute_c
				.getManyO_ATTRsOnR105(ClassIdentifierAttribute_c.getManyO_OIDAsOnR110(ReferredToIdentifierAttribute_c
						.getManyO_RTIDAsOnR111((AttributeReferenceInClass_c) m_inst)));

		m_AttributeReferredToAttribute = v_attributeReferredToAttribute;
		num_children += m_AttributeReferredToAttribute.length;

		sorter.sort(v_attributeReferredToAttribute);
		Association_c[] v_associationReferencedOver = Association_c
				.getManyR_RELsOnR201(ClassInAssociation_c.getManyR_OIRsOnR203(ReferredToClassInAssoc_c.getManyR_RTOsOnR110(ReferredToIdentifierAttribute_c
						.getManyO_RTIDAsOnR111((AttributeReferenceInClass_c) m_inst))));

		m_AssociationReferencedOver = v_associationReferencedOver;
		num_children += m_AssociationReferencedOver.length;

		sorter.sort(v_associationReferencedOver);
		if (m_propertyDescriptors == null
				|| 2 + num_children != m_propertyDescriptors.length) {
			m_propertyDescriptors = new PropertyDescriptor[2 + num_children];
			boolean readonly = false;

			m_propertyDescriptors[0] = new BooleanPropertyDescriptor(
					"Is_Cstrd", "Is Constrained", readonly);
			m_propertyDescriptors[0]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);
			m_propertyDescriptors[1] = new DescriptionPropertyDescriptor(
					"Descrip", "Attribute Reference in Class Description",
					m_inst);
			m_propertyDescriptors[1]
					.setCategory(BridgepointPropertySheetPage.basicCategoryName);

			int d_index = 2;
			for (int i = 0; i < m_ClassReferredToClass.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"ClassReferredToClass." + String.valueOf(i),
						"Referred To Class");
				m_propertyDescriptors[d_index].setCategory("Class");
				++d_index;
			}

			for (int i = 0; i < m_AttributeReferredToAttribute.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"AttributeReferredToAttribute." + String.valueOf(i),
						"Referred To Attribute");
				m_propertyDescriptors[d_index].setCategory("Attribute");
				++d_index;
			}

			for (int i = 0; i < m_AssociationReferencedOver.length; ++i) {
				m_propertyDescriptors[d_index] = new PropertyDescriptor(
						"AssociationReferencedOver." + String.valueOf(i),
						"Referenced Over");
				m_propertyDescriptors[d_index].setCategory("Association");
				++d_index;
			}

		}
		return m_propertyDescriptors;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#getPropertyValue(java.lang.Object)
	 */
	public Object getPropertyValue(Object id) {
		if (m_inst.isOrphaned()) {
			return "";
		}
		if (id.equals("Is_Cstrd")) {
			return new Boolean(m_inst.getIs_cstrd());
		} else if (id.equals("Descrip")) {
			return m_inst.getDescrip().replace('\n', '/');
		}
		int d_index = 2;
		for (int i = 0; i < m_ClassReferredToClass.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new ClassO_OBJPropertySource(m_ClassReferredToClass[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_AttributeReferredToAttribute.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new AttributeO_ATTRPropertySource(
						m_AttributeReferredToAttribute[i]);
			}
			++d_index;
		}

		for (int i = 0; i < m_AssociationReferencedOver.length; ++i) {
			if (m_propertyDescriptors[d_index].getId().equals(id)) {
				return new AssociationR_RELPropertySource(
						m_AssociationReferencedOver[i]);
			}
			++d_index;
		}

		return null;
	}

	/* (non-Javadoc)
	 * @see org.eclipse.ui.views.properties.IPropertySource#setPropertyValue(java.lang.Object, java.lang.Object)
	 */
	public void setPropertyValue(Object id, Object value) {
		boolean value_changed = false;
		String trans_name = "";
		Transaction tr = null;
		Ooaofooa modelRoot = (Ooaofooa) m_inst.getModelRoot();
		TransactionManager tm = TransactionManager.getSingleton();
		try {
			if (id.equals("Is_Cstrd")) {
				boolean new_val = ((Boolean) value).booleanValue();
				if (new_val != m_inst.getIs_cstrd()) {
					trans_name = "Change in property: 'Is_Cstrd' of Attribute Reference in Class"; //$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setIs_cstrd(new_val);
				}
			} else if (id.equals("Descrip")) {
				if (!value.toString().equals(m_inst.getDescrip())) {
					trans_name = "Change in property: 'Descrip' of Attribute Reference in Class";//$NON-NLS-1$
					tr = tm.startTransaction(trans_name,
							Ooaofooa.getDefaultInstance());
					value_changed = true;
					m_inst.setDescrip(value.toString());
				}
			}

			// catch all exceptions and cancel the transaction
		} catch (Exception e) {
			// this can be null if there was
			// an exception starting the
			// transaction
			if (tr != null) {
				tm.cancelTransaction(tr, e);
				tr = null;
			}
			CorePlugin.logError("Transaction: " + trans_name
					+ " could not complete", e);
		}
		if (tr != null) {
			if (value_changed)
				tm.endTransaction(tr);
			else
				tm.cancelTransaction(tr);
		}
	}
}
