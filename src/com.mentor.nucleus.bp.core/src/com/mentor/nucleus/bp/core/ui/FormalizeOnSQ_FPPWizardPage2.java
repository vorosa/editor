package com.mentor.nucleus.bp.core.ui;
//====================================================================
//
// File:    FormalizeOnSQ_FPPWizardPage2.java
//
// WARNING: Do not edit this generated file
// Generated by arc/page.inc
//
// (c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.
//
//====================================================================
import org.eclipse.jface.viewers.IStructuredSelection;
import org.eclipse.jface.viewers.StructuredSelection;
import org.eclipse.jface.wizard.IWizardPage;
import org.eclipse.swt.SWT;
import org.eclipse.swt.events.ModifyEvent;
import org.eclipse.swt.events.ModifyListener;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.layout.GridLayout;
import org.eclipse.swt.widgets.Combo;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Event;
import org.eclipse.swt.widgets.Label;
import org.eclipse.swt.widgets.Listener;
import org.eclipse.swt.widgets.Text;
import org.eclipse.ui.IWorkbench;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.ClassQueryInterface_c;

import java.util.ArrayList;
import java.util.List;

public class FormalizeOnSQ_FPPWizardPage2 extends PtWizardPage
		implements
			Listener {
	public static final String copyright = "(c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.";

	IWorkbench workbench;
	IStructuredSelection selection;

	// widgets on this page (public for unit tests)
	public Combo PackageCombo;
	public Label PackageLabel;

	// cache for user choices
	FunctionPackage_c Package = null;
	FunctionPackage_c[] PackageInstances;

	/**
	 * Constructors for FormalizeOnSQ_FPPWizardPage2.
	 */
	public FormalizeOnSQ_FPPWizardPage2() {
		super("");
		init();
	}

	public FormalizeOnSQ_FPPWizardPage2(String name) {
		super(name);
		init();
	}

	private void init() {
		setTitle("Formalize");
		setDescription("Choose the Function Package that this instance should represent");
	}

	public void onPageEntry() {
		Ooaofooa modelRoot = Selection
				.getModelRoot((StructuredSelection) ((FormalizeOnSQ_FPPWizard) getWizard())
						.getSelection());

		// cache for context
		FunctionPackageParticipant_c v_fpp = ((FormalizeOnSQ_FPPWizard) getWizard()).v_fpp;

		FunctionPackage_c v_fpk = FunctionPackage_c.getOneS_FPKOnR932(v_fpp);

		java.util.UUID v_existingId = Gd_c.Null_unique_id();

		if (((v_fpk != null))) {

			v_existingId = v_fpk.getFunpack_id();

		}

		InteractionParticipant_c v_participant = InteractionParticipant_c
				.getOneSQ_POnR930(v_fpp);

		PackageableElement_c v_packageableElem = PackageableElement_c
				.getOnePE_PEOnR8001(v_participant);

		Package_c v_package = Package_c.getOneEP_PKGOnR8000(v_packageableElem);

		Component_c v_component = Component_c
				.getOneC_COnR8003(v_packageableElem);

		boolean v_isInGenericPackage = (v_package != null)
				|| (v_component != null);

		Sequence_c v_sequence = Sequence_c.getOneSQ_SOnR929(v_participant);

		Communication_c v_communication = Communication_c
				.getOneCOMM_COMMOnR1126(v_participant);

		String v_domName = "";

		if (((v_sequence != null))) {

			v_domName = v_sequence.Getpath(true);

		}

		else if (((v_communication != null))) {

			v_domName = v_communication.Getpath(true);

		}

		class Domain_test9785_c implements ClassQueryInterface_c {
			Domain_test9785_c(String p9786) {
				m_p9786 = p9786;
			}
			private String m_p9786;
			public boolean evaluate(Object candidate) {
				Domain_c selected = (Domain_c) candidate;
				return (selected.getName().equals(m_p9786));
			}
		}
		Domain_c v_domain = Domain_c.DomainInstance(modelRoot,
				new Domain_test9785_c(v_domName));

		if (((v_domain != null))) {

			// select related by where USER::selectOne
			FunctionPackage_c[] v_PackageInstances = FunctionPackage_c
					.getManyS_FPKsOnR301(FunctionPackageInDomain_c
							.getManyPL_FPIDsOnR301(v_domain));
			FunctionPackage_c v_Package = null;
			int PackageInstCount;
			int PackageResultCount = 0;
			for (PackageInstCount = 0; PackageInstCount < v_PackageInstances.length; PackageInstCount++) {
				FunctionPackage_c selected = v_PackageInstances[PackageInstCount];
				if (User_c.Selectone(selected.getFunpack_id())
						&& !selected.getFunpack_id().equals(v_existingId)) {
					PackageResultCount++;
				}
			}
			this.PackageInstances = new FunctionPackage_c[PackageResultCount];
			PackageResultCount = 0;
			for (PackageInstCount = 0; PackageInstCount < v_PackageInstances.length; PackageInstCount++) {
				FunctionPackage_c selected = v_PackageInstances[PackageInstCount];
				if (User_c.Selectone(selected.getFunpack_id())
						&& !selected.getFunpack_id().equals(v_existingId)) {
					this.PackageInstances[PackageResultCount] = selected;
					PackageResultCount++;
				}
			}
			PackageCombo.removeAll();
			for (PackageInstCount = 0; PackageInstCount < PackageResultCount; PackageInstCount++)
				PackageCombo
						.add(((FunctionPackage_c) this.PackageInstances[PackageInstCount])
								.getName());
			if (PackageResultCount == 1) {
				PackageCombo.select(0);
				updateSelectedPackage();
			}

			if (((v_Package != null))) {

			}

		}

	}

	public void createControl(Composite parent) {
		// create the composite to hold the widgets   
		GridData gd = null;
		Composite composite = new Composite(parent, SWT.NULL);

		// create the desired layout for this wizard page
		GridLayout gl = new GridLayout();
		int ncol = 5;
		gl.numColumns = ncol;
		composite.setLayout(gl);

		PackageLabel = new Label(composite, SWT.NONE);
		PackageLabel.setText("Package");
		PackageCombo = new Combo(composite, SWT.BORDER | SWT.READ_ONLY);
		PackageCombo.setLayoutData(new GridData(GridData.FILL_HORIZONTAL));

		// set the composite as the control for this page
		setControl(composite);
		onPageEntry(); // Initialize the ui widget contents
		addListeners();
	}

	private void addListeners() {
		PackageCombo.addListener(SWT.Selection, this);
	}

	// public for unit tests
	public void updateSelectedPackage() {
		this.Package = PackageInstances[PackageCombo.getSelectionIndex()];
		((FormalizeOnSQ_FPPWizard) getWizard()).v_Package = this.Package;
	}

	/**
	 * @see Listener#handleEvent(Event)
	 */
	public void handleEvent(Event event) {
		if (event.widget == PackageCombo) {
			updateSelectedPackage();
		}
		getWizard().getContainer().updateButtons();
	}

	public boolean isPageComplete() {
		boolean isPageComplete = true;
		if (Package == null)
			isPageComplete = false;
		return isPageComplete;
	}

	public IWizardPage getNextPage() {
		((FormalizeOnSQ_FPPWizard) getWizard()).v_Package = Package;
		PtWizardPage page = (PtWizardPage) getWizard().getNextPage(this);
		page.onPageEntry();
		return page;
	}

	/**
	 * @see IWizardPage#canFlipToNextPage()
	 */
	public boolean canFlipToNextPage() {
		if ((PtWizardPage) getWizard().getNextPage(this) == null)
			return false;
		if (Package == null)
			return false;
		return true;
	}

}
