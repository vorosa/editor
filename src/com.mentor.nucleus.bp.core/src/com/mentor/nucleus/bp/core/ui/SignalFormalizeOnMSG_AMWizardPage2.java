package com.mentor.nucleus.bp.core.ui;
//====================================================================
//
// File:    SignalFormalizeOnMSG_AMWizardPage2.java
//
// WARNING: Do not edit this generated file
// Generated by arc/page.inc
//
// (c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.
//
//====================================================================
import org.eclipse.jface.viewers.IStructuredSelection;
import org.eclipse.jface.viewers.StructuredSelection;
import org.eclipse.jface.wizard.IWizardPage;
import org.eclipse.swt.SWT;
import org.eclipse.swt.events.ModifyEvent;
import org.eclipse.swt.events.ModifyListener;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.layout.GridLayout;
import org.eclipse.swt.widgets.Combo;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Event;
import org.eclipse.swt.widgets.Label;
import org.eclipse.swt.widgets.Listener;
import org.eclipse.swt.widgets.Text;
import org.eclipse.ui.IWorkbench;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.ClassQueryInterface_c;

import java.util.ArrayList;
import java.util.List;

public class SignalFormalizeOnMSG_AMWizardPage2 extends PtWizardPage
		implements
			Listener {
	public static final String copyright = "(c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.";

	IWorkbench workbench;
	IStructuredSelection selection;

	// widgets on this page (public for unit tests)
	public Combo SignalCombo;
	public Label SignalLabel;

	// cache for user choices
	InterfaceSignal_c Signal = null;
	InterfaceSignal_c[] SignalInstances;

	/**
	 * Constructors for SignalFormalizeOnMSG_AMWizardPage2.
	 */
	public SignalFormalizeOnMSG_AMWizardPage2() {
		super("");
		init();
	}

	public SignalFormalizeOnMSG_AMWizardPage2(String name) {
		super(name);
		init();
	}

	private void init() {
		setTitle("Formalize");
		setDescription("Choose the message to send");
	}

	public void onPageEntry() {
		Ooaofooa modelRoot = Selection
				.getModelRoot((StructuredSelection) ((SignalFormalizeOnMSG_AMWizard) getWizard())
						.getSelection());

		// cache for context
		AsynchronousMessage_c v_asyncMessage = ((SignalFormalizeOnMSG_AMWizard) getWizard()).v_asyncMessage;

		Message_c v_message = Message_c.getOneMSG_MOnR1018(v_asyncMessage);

		InteractionParticipant_c v_participant = InteractionParticipant_c
				.getOneSQ_POnR1007(v_message);

		ComponentParticipant_c v_cop = ComponentParticipant_c
				.getOneSQ_COPOnR930(v_participant);

		Component_c v_component = (Component_c) modelRoot.getInstanceList(
				Component_c.class).getGlobal(null, Gd_c.Null_unique_id());

		if (((v_cop == null))) {

			v_cop = ComponentParticipant_c
					.getOneSQ_COPOnR930(InteractionParticipant_c
							.getOneSQ_POnR940(Lifespan_c
									.getOneSQ_LSOnR930(v_participant)));

		}

		if (((v_cop != null))) {

			v_component = Component_c.getOneC_COnR955(v_cop);

		}

		if (((v_component != null))) {

			// select related by where USER::selectOne
			InterfaceSignal_c[] v_SignalInstances = InterfaceSignal_c
					.getManyC_ASsOnR4004(ExecutableProperty_c.getManyC_EPsOnR4003(Interface_c
							.getManyC_IsOnR4012(InterfaceReference_c
									.getManyC_IRsOnR4016(Port_c
											.getManyC_POsOnR4010(v_component)))));
			InterfaceSignal_c v_Signal = null;
			int SignalInstCount;
			int SignalResultCount = 0;
			for (SignalInstCount = 0; SignalInstCount < v_SignalInstances.length; SignalInstCount++) {
				InterfaceSignal_c selected = v_SignalInstances[SignalInstCount];
				if (User_c.Selectone(selected.getId())
						&& v_asyncMessage
								.Canformalizeusinginterfacesignal(selected
										.getId())) {
					SignalResultCount++;
				}
			}
			this.SignalInstances = new InterfaceSignal_c[SignalResultCount];
			SignalResultCount = 0;
			for (SignalInstCount = 0; SignalInstCount < v_SignalInstances.length; SignalInstCount++) {
				InterfaceSignal_c selected = v_SignalInstances[SignalInstCount];
				if (User_c.Selectone(selected.getId())
						&& v_asyncMessage
								.Canformalizeusinginterfacesignal(selected
										.getId())) {
					this.SignalInstances[SignalResultCount] = selected;
					SignalResultCount++;
				}
			}
			SignalCombo.removeAll();
			for (SignalInstCount = 0; SignalInstCount < SignalResultCount; SignalInstCount++)
				SignalCombo
						.add(((InterfaceSignal_c) this.SignalInstances[SignalInstCount])
								.getName());
			if (SignalResultCount == 1) {
				SignalCombo.select(0);
				updateSelectedSignal();
			}

			if (((v_Signal != null))) {

			}

		}

	}

	public void createControl(Composite parent) {
		// create the composite to hold the widgets   
		GridData gd = null;
		Composite composite = new Composite(parent, SWT.NULL);

		// create the desired layout for this wizard page
		GridLayout gl = new GridLayout();
		int ncol = 5;
		gl.numColumns = ncol;
		composite.setLayout(gl);

		SignalLabel = new Label(composite, SWT.NONE);
		SignalLabel.setText("Signal");
		SignalCombo = new Combo(composite, SWT.BORDER | SWT.READ_ONLY);
		SignalCombo.setLayoutData(new GridData(GridData.FILL_HORIZONTAL));

		// set the composite as the control for this page
		setControl(composite);
		onPageEntry(); // Initialize the ui widget contents
		addListeners();
	}

	private void addListeners() {
		SignalCombo.addListener(SWT.Selection, this);
	}

	// public for unit tests
	public void updateSelectedSignal() {
		this.Signal = SignalInstances[SignalCombo.getSelectionIndex()];
		((SignalFormalizeOnMSG_AMWizard) getWizard()).v_Signal = this.Signal;
	}

	/**
	 * @see Listener#handleEvent(Event)
	 */
	public void handleEvent(Event event) {
		if (event.widget == SignalCombo) {
			updateSelectedSignal();
		}
		getWizard().getContainer().updateButtons();
	}

	public boolean isPageComplete() {
		boolean isPageComplete = true;
		if (Signal == null)
			isPageComplete = false;
		return isPageComplete;
	}

	public IWizardPage getNextPage() {
		((SignalFormalizeOnMSG_AMWizard) getWizard()).v_Signal = Signal;
		PtWizardPage page = (PtWizardPage) getWizard().getNextPage(this);
		page.onPageEntry();
		return page;
	}

	/**
	 * @see IWizardPage#canFlipToNextPage()
	 */
	public boolean canFlipToNextPage() {
		if ((PtWizardPage) getWizard().getNextPage(this) == null)
			return false;
		if (Signal == null)
			return false;
		return true;
	}

}
