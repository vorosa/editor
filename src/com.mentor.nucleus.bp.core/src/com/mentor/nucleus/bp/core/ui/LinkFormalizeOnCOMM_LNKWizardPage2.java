package com.mentor.nucleus.bp.core.ui;
//====================================================================
//
// File:    LinkFormalizeOnCOMM_LNKWizardPage2.java
//
// WARNING: Do not edit this generated file
// Generated by arc/page.inc
//
// (c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.
//
//====================================================================
import org.eclipse.jface.viewers.IStructuredSelection;
import org.eclipse.jface.viewers.StructuredSelection;
import org.eclipse.jface.wizard.IWizardPage;
import org.eclipse.swt.SWT;
import org.eclipse.swt.events.ModifyEvent;
import org.eclipse.swt.events.ModifyListener;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.layout.GridLayout;
import org.eclipse.swt.widgets.Combo;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Event;
import org.eclipse.swt.widgets.Label;
import org.eclipse.swt.widgets.Listener;
import org.eclipse.swt.widgets.Text;
import org.eclipse.ui.IWorkbench;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.ClassQueryInterface_c;

import java.util.ArrayList;
import java.util.List;

public class LinkFormalizeOnCOMM_LNKWizardPage2 extends PtWizardPage
		implements
			Listener {
	public static final String copyright = "(c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.";

	IWorkbench workbench;
	IStructuredSelection selection;

	// widgets on this page (public for unit tests)
	public Combo AssociationCombo;
	public Label AssociationLabel;

	// cache for user choices
	Association_c Association = null;
	Association_c[] AssociationInstances;

	/**
	 * Constructors for LinkFormalizeOnCOMM_LNKWizardPage2.
	 */
	public LinkFormalizeOnCOMM_LNKWizardPage2() {
		super("");
		init();
	}

	public LinkFormalizeOnCOMM_LNKWizardPage2(String name) {
		super(name);
		init();
	}

	private void init() {
		setTitle("Formalize");
		setDescription("Choose the association to formalize with");
	}

	public void onPageEntry() {
		Ooaofooa modelRoot = Selection
				.getModelRoot((StructuredSelection) ((LinkFormalizeOnCOMM_LNKWizard) getWizard())
						.getSelection());

		// cache for context
		CommunicationLink_c v_link = ((LinkFormalizeOnCOMM_LNKWizard) getWizard()).v_link;

		if (((v_link != null))) {

			ModelClass_c v_startObj = ModelClass_c
					.getOneO_OBJOnR934(ClassInstanceParticipant_c
							.getOneSQ_CIPOnR930(InteractionParticipant_c
									.getOneSQ_POnR1133(v_link)));

			// select related by where USER::selectOne
			Association_c[] v_AssociationInstances = Association_c
					.getManyR_RELsOnR201(ClassInAssociation_c
							.getManyR_OIRsOnR201(v_startObj));
			Association_c v_Association = null;
			int AssociationInstCount;
			int AssociationResultCount = 0;
			for (AssociationInstCount = 0; AssociationInstCount < v_AssociationInstances.length; AssociationInstCount++) {
				Association_c selected = v_AssociationInstances[AssociationInstCount];
				if (User_c.Selectone(selected.getRel_id())
						&& v_link.Participateswithformalparticipants(selected
								.getRel_id())) {
					AssociationResultCount++;
				}
			}
			this.AssociationInstances = new Association_c[AssociationResultCount];
			AssociationResultCount = 0;
			for (AssociationInstCount = 0; AssociationInstCount < v_AssociationInstances.length; AssociationInstCount++) {
				Association_c selected = v_AssociationInstances[AssociationInstCount];
				if (User_c.Selectone(selected.getRel_id())
						&& v_link.Participateswithformalparticipants(selected
								.getRel_id())) {
					this.AssociationInstances[AssociationResultCount] = selected;
					AssociationResultCount++;
				}
			}
			AssociationCombo.removeAll();
			for (AssociationInstCount = 0; AssociationInstCount < AssociationResultCount; AssociationInstCount++)
				AssociationCombo
						.add(((Association_c) this.AssociationInstances[AssociationInstCount])
								.Get_name());
			if (AssociationResultCount == 1) {
				AssociationCombo.select(0);
				updateSelectedAssociation();
			}

		}

	}

	public void createControl(Composite parent) {
		// create the composite to hold the widgets   
		GridData gd = null;
		Composite composite = new Composite(parent, SWT.NULL);

		// create the desired layout for this wizard page
		GridLayout gl = new GridLayout();
		int ncol = 5;
		gl.numColumns = ncol;
		composite.setLayout(gl);

		AssociationLabel = new Label(composite, SWT.NONE);
		AssociationLabel.setText("Association");
		AssociationCombo = new Combo(composite, SWT.BORDER | SWT.READ_ONLY);
		AssociationCombo.setLayoutData(new GridData(GridData.FILL_HORIZONTAL));

		// set the composite as the control for this page
		setControl(composite);
		onPageEntry(); // Initialize the ui widget contents
		addListeners();
	}

	private void addListeners() {
		AssociationCombo.addListener(SWT.Selection, this);
	}

	// public for unit tests
	public void updateSelectedAssociation() {
		this.Association = AssociationInstances[AssociationCombo
				.getSelectionIndex()];
		((LinkFormalizeOnCOMM_LNKWizard) getWizard()).v_Association = this.Association;
	}

	/**
	 * @see Listener#handleEvent(Event)
	 */
	public void handleEvent(Event event) {
		if (event.widget == AssociationCombo) {
			updateSelectedAssociation();
		}
		getWizard().getContainer().updateButtons();
	}

	public boolean isPageComplete() {
		boolean isPageComplete = true;
		if (Association == null)
			isPageComplete = false;
		return isPageComplete;
	}

	public IWizardPage getNextPage() {
		((LinkFormalizeOnCOMM_LNKWizard) getWizard()).v_Association = Association;
		PtWizardPage page = (PtWizardPage) getWizard().getNextPage(this);
		page.onPageEntry();
		return page;
	}

	/**
	 * @see IWizardPage#canFlipToNextPage()
	 */
	public boolean canFlipToNextPage() {
		if ((PtWizardPage) getWizard().getNextPage(this) == null)
			return false;
		if (Association == null)
			return false;
		return true;
	}

}
