package com.mentor.nucleus.bp.core.ui;
//====================================================================
//
// File:    SpecializedPackageAssignSignalOnSM_TXNWizard.java
//
// WARNING: Do not edit this generated file
// Generated by arc/wizard.inc
//
// (c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.
//
//====================================================================
import java.util.Iterator;

import org.eclipse.jface.viewers.IStructuredSelection;
import org.eclipse.jface.viewers.Viewer;
import org.eclipse.jface.viewers.StructuredViewer;
import org.eclipse.jface.wizard.*;
import org.eclipse.ui.IWorkbench;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.*;
import com.mentor.nucleus.bp.core.util.TransactionUtil;

import java.util.ArrayList;
import java.util.List;
import java.util.Stack;

/**
 * SpecializedPackageAssignSignalOnSM_TXNWizard class
 */
public class SpecializedPackageAssignSignalOnSM_TXNWizard extends Wizard {
	public static final String copyright = "(c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.";

	// workbench selection when the wizard was started
	protected IStructuredSelection m_selection;

	// the workbench instance
	protected IWorkbench m_workbench;

	// the viewer the wizard was started from. This must be replaced with a listener system
	protected Viewer m_viewer;

	// Pages stack
	protected Stack m_pagesStack = new Stack();

	// wizard pages
	SpecializedPackageAssignSignalOnSM_TXNWizardPage1 SM_TXN_SpecializedPackageAssignSignalPage1;
	SpecializedPackageAssignSignalOnSM_TXNWizardPage2 SM_TXN_SpecializedPackageAssignSignalPage2;

	// wizard state
	// constants
	protected static final int INITIAL_WIZARD_STATE = -1;
	protected static final int SM_TXN_SPECIALIZEDPACKAGEASSIGNSIGNAL0_WIZARD_STATE = 0;
	protected static final int SM_TXN_SPECIALIZEDPACKAGEASSIGNSIGNAL1_WIZARD_STATE = 1;
	// member
	protected int m_state = INITIAL_WIZARD_STATE;
	// end wizard state

	// cache for contextual selections (public for use by unit tests)
	public Transition_c v_txn = null;

	// cache for the users selections (public for use by unit tests)
	public InterfaceReference_c v_interface = null;
	public InterfaceSignal_c v_signal = null;

	/**
	 * Constructor for SpecializedPackageAssignSignalOnSM_TXNWizard.
	 */
	public SpecializedPackageAssignSignalOnSM_TXNWizard() {
		super();
	}

	/**
	 * See field.
	 */
	public IStructuredSelection getSelection() {
		return m_selection;
	}

	public void addPages() {
		SM_TXN_SpecializedPackageAssignSignalPage1 = new SpecializedPackageAssignSignalOnSM_TXNWizardPage1(
				"SpecializedPackageAssignSignalOnSM_TXNWizardPage1");
		addPage(SM_TXN_SpecializedPackageAssignSignalPage1);
		SM_TXN_SpecializedPackageAssignSignalPage2 = new SpecializedPackageAssignSignalOnSM_TXNWizardPage2(
				"SpecializedPackageAssignSignalOnSM_TXNWizardPage2");
		addPage(SM_TXN_SpecializedPackageAssignSignalPage2);
	}

	/**
	 * @see IWorkbenchWizard#init(IWorkbench, IStructuredSelection)
	 */
	public void init(IWorkbench workbench, IStructuredSelection selection,
			Viewer viewer) {
		this.m_workbench = workbench;
		this.m_selection = selection;
		this.m_viewer = viewer;
		for (Iterator iterator = selection.iterator(); iterator.hasNext();) {
			Object context = iterator.next();
			if (context instanceof Transition_c) {
				v_txn = (Transition_c) context;
			}
		}
	}

	public boolean canFinish() {
		boolean pagesComplete = true;
		pagesComplete = pagesComplete
				&& SM_TXN_SpecializedPackageAssignSignalPage1.isPageComplete();
		pagesComplete = pagesComplete
				&& SM_TXN_SpecializedPackageAssignSignalPage2.isPageComplete();
		return pagesComplete;
	}

	public IWizardPage getNextPage(IWizardPage page) {
		List pages = new ArrayList(getPages().length);
		for (int i = 0; i < getPages().length; i++) {
			pages.add(getPages()[i]);
		}
		switch (pages.indexOf(page)) {
			case SM_TXN_SPECIALIZEDPACKAGEASSIGNSIGNAL0_WIZARD_STATE :
				m_state = pages
						.indexOf(SM_TXN_SpecializedPackageAssignSignalPage2);
				SM_TXN_SpecializedPackageAssignSignalPage2.onPageEntry();
				return SM_TXN_SpecializedPackageAssignSignalPage2;

			case SM_TXN_SPECIALIZEDPACKAGEASSIGNSIGNAL1_WIZARD_STATE :
				return null;
		}
		return null;
	}

	public IWizardPage getPreviousPage(IWizardPage page) {
		if (m_pagesStack.empty()) {
			return null;
		} else {
			List pages = new ArrayList(getPages().length);
			for (int i = 0; i < getPages().length; i++) {
				pages.add(getPages()[i]);
			}
			m_state = pages.indexOf((IWizardPage) m_pagesStack.peek());
			((PtWizardPage) m_pagesStack.peek()).onPageEntry();
			return (IWizardPage) m_pagesStack.pop();
		}
	}

	public boolean performFinish() {
		TransactionUtil.TransactionGroup transactionGroup = null;
		ModelRoot modelRoot = v_txn.getModelRoot();
		try {
			transactionGroup = TransactionUtil
					.startTransactionsOnSelectedModelRoots("Specialized Package Assign Signal"); //$NON-NLS-1$
			java.util.UUID v_Trans_ID = v_txn.getTrans_id();

			StateMachine_c v_stateMachine = StateMachine_c
					.getOneSM_SMOnR505(v_txn);

			ClassStateMachine_c v_classStateMachine = ClassStateMachine_c
					.getOneSM_ASMOnR517(v_stateMachine);

			if ((v_interface != null)) {

				if (v_txn != null) {
					v_txn.Addsignal(v_interface.getId(), v_signal.getId());
				} else {
					Throwable t = new Throwable();
					t.fillInStackTrace();
					CorePlugin
							.logError(
									"Attempted to call an operation on a null instance.",
									t);
				}

			}

			// catch all exceptions and cancel the transaction
		} catch (Exception e) {
			if (transactionGroup != null)
				TransactionUtil.cancelTransactions(transactionGroup, e);
			CorePlugin.logError(
					"Transaction: Specialized Package Assign Signal failed", e);//$NON-NLS-1$
			// return true so that the wizard will
			// close
			return true;
		}
		if (transactionGroup != null)
			TransactionUtil.endTransactions(transactionGroup);
		if (m_viewer != null) {
			if (m_viewer instanceof StructuredViewer) {
				((StructuredViewer) m_viewer).refresh(v_txn);
			} else {
				m_viewer.refresh();
			}
		}
		return true;
	}
}
