      
package com.mentor.nucleus.bp.core ;
//====================================================================
//
// File:      com.mentor.nucleus.bp.core.MooreStateMachine_c.java
//
// WARNING: Do not edit this generated file
// Generated by ../MC-Java/java.arc, $Revision: 1.111 $
//
// (c) Copyright 2005-2014 by Mentor Graphics Corp.  All rights reserved.
//
//====================================================================

// No special imports
import java.util.* ;
import java.lang.reflect.*;
import org.eclipse.core.resources.IFile;
import org.eclipse.core.resources.IResource;
import org.eclipse.core.runtime.IAdaptable;
import org.eclipse.core.runtime.IPath;
import org.eclipse.core.runtime.Path;
import com.mentor.nucleus.bp.core.util.PersistenceUtil;
import org.eclipse.core.runtime.NullProgressMonitor;
import com.mentor.nucleus.bp.core.ui.marker.UmlProblem;
import com.mentor.nucleus.bp.core.common.*;      
abstract class EV_MOORE_STATE_MACHINE extends genericEvent_c 
{
  public abstract int getEvtcode() ;
}

public class MooreStateMachine_c extends NonRootModelElement implements IAdaptable, Cloneable
{
  // Public Constructors
  public MooreStateMachine_c(ModelRoot modelRoot,
       java.util.UUID         p_m_sm_id)
  {
    super(modelRoot);
       //pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
       m_sm_id = IdAssigner.preprocessUUID(p_m_sm_id);
        
    Object [] key = {m_sm_id};
    addInstanceToMap(key);
  }
  static public MooreStateMachine_c createProxy(ModelRoot modelRoot,
       java.util.UUID         p_m_sm_id, String p_contentPath, IPath p_localPath)
  {
	  ModelRoot resolvedModelRoot = ModelRoot.findModelRoot(modelRoot, p_contentPath, p_localPath);
	  // if a model root was not resolved it is most likely
	  // due to a missing file of the proxy, defualt back to
	  // the original model root
	  if(resolvedModelRoot != null)
	  	modelRoot = resolvedModelRoot;
      InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
      MooreStateMachine_c new_inst = null;
      synchronized(instances) {
          Object[] key = {p_m_sm_id};
          new_inst = (MooreStateMachine_c) instances.get(key) ;
        }
	String contentPath = PersistenceUtil.resolveRelativePath(
			p_localPath,
			new Path(p_contentPath));
	if(modelRoot.isNewCompareRoot()) {
	      // for comparisons we do not want to change
	      // the content path
	      contentPath = p_contentPath;
	}
	if ( new_inst != null && !modelRoot.isCompareRoot()) {
		PersistableModelComponent pmc = new_inst.getPersistableComponent();
		if (pmc == null) {
			// dangling reference, redo this instance
			new_inst.batchUnrelate();
       //pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
       new_inst.m_sm_id = IdAssigner.preprocessUUID(p_m_sm_id);
        
		}
	}
	if ( new_inst == null ) {
		// there is no instance matching the id, create a proxy
		// if the resource doesn't exist then this will be a dangling reference
    	new_inst = new MooreStateMachine_c(modelRoot,
p_m_sm_id
);
		new_inst.m_contentPath = contentPath;
	}
    return new_inst;
  }

  static public MooreStateMachine_c resolveInstance(ModelRoot modelRoot,
       java.util.UUID         p_m_sm_id){
    InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
    MooreStateMachine_c source = null;
    synchronized(instances) {
        Object [] key = {
        p_m_sm_id
            };
        source = (MooreStateMachine_c) instances.get(key);
        if (source != null && !modelRoot.isCompareRoot()) {
           source.convertFromProxy();
           source.batchUnrelate();
       //pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
       source.m_sm_id = IdAssigner.preprocessUUID(p_m_sm_id);
        
           return source ;
        }
      }
      // there is no instance matching the id
    MooreStateMachine_c new_inst = new MooreStateMachine_c(modelRoot,
p_m_sm_id
);
    return new_inst;
  }
  public MooreStateMachine_c(ModelRoot modelRoot)
  {
    super(modelRoot);
     m_sm_id = IdAssigner.NULL_UUID;   
    Object [] key = {m_sm_id};
    addInstanceToMap(key);
  }

  public Object getInstanceKey() {
    Object [] key =  {m_sm_id};
		return key;
  }
  
  public boolean setInstanceKey(UUID p_newKey){
  
  	boolean changed = false;
		// round p1
		// round p2
		// round p3
			// round p4
			// round p5
	if (m_sm_id !=  p_newKey ){
	
		m_sm_id = p_newKey; 	
		changed = true;
	}
  	return changed;
  }
  

  



  public boolean equals (Object elem) {
     if (!(elem instanceof MooreStateMachine_c)) {
         return false;
     }
	    // check that the model-roots are the same
    	if (((NonRootModelElement)elem).getModelRoot() != getModelRoot()) {
    		return false;
    	}
    	
     return identityEquals(elem);
  }
  
   public boolean identityEquals(Object elem) {
       if (!(elem instanceof MooreStateMachine_c)) {
           return false;
       }
       
       MooreStateMachine_c me = (MooreStateMachine_c)elem;
      // don't allow an empty id-value to produce a false positive result;
      // in this case, use whether the two instances are actually the same 
      // one in memory, instead
      if ((IdAssigner.NULL_UUID.equals(getSm_id()) || IdAssigner.NULL_UUID.equals(((MooreStateMachine_c)elem).getSm_id())) && this != elem) {
      	return false;
      }
      if (!getSm_id().equals(((MooreStateMachine_c)elem).getSm_id())) return false;
      return true;
    }

   public boolean cachedIdentityEquals(Object elem) {
       if (!(elem instanceof MooreStateMachine_c)) {
           return false;
       }
       
       MooreStateMachine_c me = (MooreStateMachine_c)elem;
      if (!getSm_idCachedValue().equals(((MooreStateMachine_c)elem).getSm_idCachedValue())) return false;
      return true;
    }


  // Attributes
  private java.util.UUID         m_sm_id ;
      
// declare association references from this class
  
  
    
// referring navigation

StateMachine_c IsSupertypeStateMachine ;
public void relateAcrossR510To(StateMachine_c target)
{
	relateAcrossR510To(target, true);
}
public void relateAcrossR510To(StateMachine_c target, boolean notifyChanges)
{
	if (target == null) return;
	
	if (target == IsSupertypeStateMachine) return;  // already related

    if ( IsSupertypeStateMachine != target ) {
    
    Object oldKey = getInstanceKey();

    if (IsSupertypeStateMachine != null) {
    
        IsSupertypeStateMachine.clearBackPointerR510To(this);
        
		if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == true) {   //$NON-NLS-1$
		Ooaofooa.log.println(ILogger.CONSISTENCY, 
			"MooreStateMachine_c.relateAcrossR510To(StateMachine_c target)", 
			"Relate performed across R510 from Moore State Machine to State Machine without unrelate of prior instance.");  
		}
    }
                
    	IsSupertypeStateMachine = target ;
        m_sm_id = target.getSm_id();
      updateInstanceKey(oldKey, getInstanceKey());
	    target.setBackPointerR510To(this);
        target.addRef();
      if(notifyChanges) {
	    	RelationshipChangeModelDelta change = new RelationshipChangeModelDelta(Modeleventnotification_c.DELTA_ELEMENT_RELATED, this, target, "510", "");
        Ooaofooa.getDefaultInstance().fireModelElementRelationChanged(change);
      }
    }
}
public void unrelateAcrossR510From(StateMachine_c target)
{
	unrelateAcrossR510From(target, true);
}
public void unrelateAcrossR510From(StateMachine_c target, boolean notifyChanges)
{
	if (target == null) return;
	
	if (IsSupertypeStateMachine == null) return;  // already unrelated
	
	if (target != IsSupertypeStateMachine) {
		Exception e = new Exception();
		e.fillInStackTrace();
		CorePlugin.logError("Tried to unrelate from non-related instance across R510", e);
		return;
	}

    if (target != null) {
        target.clearBackPointerR510To(this);
    }
    

      if(IsSupertypeStateMachine != null) {
      
        m_sm_id = IsSupertypeStateMachine.getSm_id();
        IsSupertypeStateMachine = null ;
        target.removeRef();
      if(notifyChanges) { 
        RelationshipChangeModelDelta change = new RelationshipChangeModelDelta(Modeleventnotification_c.DELTA_ELEMENT_UNRELATED, this, target, "510", "");
        Ooaofooa.getDefaultInstance().fireModelElementRelationChanged(change);
      }
    }
}

  
    
      
public static MooreStateMachine_c getOneSM_MOOREOnR510(StateMachine_c [] targets)
{
    return getOneSM_MOOREOnR510(targets, null);
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(StateMachine_c [] targets, ClassQueryInterface_c test)
{
    MooreStateMachine_c ret_val = null;
    if (targets != null)
    {
        for (int i = 0; i < targets.length && ret_val == null; ++i )
        {
           ret_val = getOneSM_MOOREOnR510(targets[i], test);
        }
    }

    return ret_val;
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(StateMachine_c target)
{
  return getOneSM_MOOREOnR510(target, null);
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(StateMachine_c target, boolean loadComponent)
{
    return getOneSM_MOOREOnR510(target.getModelRoot(), target, null, loadComponent);
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(StateMachine_c target, ClassQueryInterface_c test)
{
  if (target != null) {
    return getOneSM_MOOREOnR510(target.getModelRoot(), target, test);
  }
  return null;
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(ModelRoot modelRoot, StateMachine_c target, ClassQueryInterface_c test)
{
	return getOneSM_MOOREOnR510(modelRoot, target, test, true);
}

public static MooreStateMachine_c getOneSM_MOOREOnR510(ModelRoot modelRoot, StateMachine_c target, ClassQueryInterface_c test, boolean loadComponent)
{
   return find_getOneSM_MOOREOnR510(modelRoot,target,test);
}
private static MooreStateMachine_c find_getOneSM_MOOREOnR510(ModelRoot modelRoot, StateMachine_c target, ClassQueryInterface_c test)
{
  if (target != null) {
    MooreStateMachine_c source = (MooreStateMachine_c)target.backPointer_IsSubtypeMooreStateMachineIsSubtype_R510;
    if (source != null && (test == null || test.evaluate(source))) {
      return source;
    }
  }
  // not found
  return null ;
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c [] targets)
{
  return getManySM_MOOREsOnR510(targets, null);
}
public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c [] targets, boolean loadComponent)
{
  return getManySM_MOOREsOnR510(targets, null, loadComponent);
}
public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c [] targets, ClassQueryInterface_c test)
{
	return getManySM_MOOREsOnR510(targets, test, true);
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c [] targets, ClassQueryInterface_c test, boolean loadComponent)
{

  if(targets == null || targets.length == 0 || targets[0] == null)
  	return new MooreStateMachine_c[0];

  ModelRoot modelRoot = targets[0].getModelRoot();
 
   InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);

  Vector matches = new Vector();
  for (int i = 0 ; i < targets.length ; i++) {
      MooreStateMachine_c source = (MooreStateMachine_c)targets[i].backPointer_IsSubtypeMooreStateMachineIsSubtype_R510;
      if (source != null && (test == null || test.evaluate(source))) {
        matches.add(source);
      }
  }
  if (matches.size() > 0) {
	MooreStateMachine_c[] ret_set = new MooreStateMachine_c[matches.size()];
	matches.copyInto(ret_set);
	return ret_set;
  } else {
	return new MooreStateMachine_c[0];
  }
  }

public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c target)
{
  if (target != null) {
    StateMachine_c [] targetArray = new StateMachine_c[1];
    targetArray[0] = target;
    return getManySM_MOOREsOnR510(targetArray);
  } else {
    MooreStateMachine_c [] result = new MooreStateMachine_c [0] ;
  return result ;
}
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR510(StateMachine_c target, boolean loadComponent)
{
  if (target != null) {
    StateMachine_c [] targetArray = new StateMachine_c[1];
    targetArray[0] = target;
    return getManySM_MOOREsOnR510(targetArray, loadComponent);
  } else {
    MooreStateMachine_c [] result = new MooreStateMachine_c [0] ;
  return result ;
}
}




// declare associations referring to this class

    
  
  
// referred to navigation

ArrayList<MooreActionHome_c> backPointer_MooreActionHome_R511 = new ArrayList<MooreActionHome_c>();

public void setMooreActionHomeOrderInChildListR511(MooreActionHome_c target, int index) {
	unrelateAcrossR511From(target);
	MooreActionHome_c[] elements = MooreActionHome_c.getManySM_MOAHsOnR511(this);
	int count = 0;
	for(int i = 0; i < elements.length; i++) {
		if(count >= index) {
			unrelateAcrossR511From((MooreActionHome_c) elements[i]); 
		}
		count++;
	}
	relateAcrossR511To(target);
	count = 0;
	for(int i = 0; i < elements.length; i++) {
		if(count >= index) {
			relateAcrossR511To((MooreActionHome_c) elements[i]);
		}
		count++;
	}
}

public void relateAcrossR511To(MooreActionHome_c target)
{
  if (target != null) {
    target.relateAcrossR511To(this, true) ;
  }
}

public void relateAcrossR511To(MooreActionHome_c target, boolean notifyChanges)
{
  if (target != null) {
    target.relateAcrossR511To(this, notifyChanges) ;
  }
}

public void setBackPointerR511To(MooreActionHome_c target)
{
  	synchronized (backPointer_MooreActionHome_R511) {
		backPointer_MooreActionHome_R511.add(target);
	}
}

public void unrelateAcrossR511From(MooreActionHome_c target)
{
  if (target != null) {
    target.unrelateAcrossR511From(this, true) ;
  }
}

public void unrelateAcrossR511From(MooreActionHome_c target, boolean notifyChanges)
{
  if (target != null) {
    target.unrelateAcrossR511From(this, notifyChanges) ;
  }
}

public void clearBackPointerR511To(MooreActionHome_c target)
{
  synchronized (backPointer_MooreActionHome_R511) {
	    backPointer_MooreActionHome_R511.remove(target);
	}
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(MooreActionHome_c [] targets)
{
    return getOneSM_MOOREOnR511(targets, null);
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(MooreActionHome_c [] targets, ClassQueryInterface_c test)
{
    return getOneSM_MOOREOnR511(targets, test, true);
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(MooreActionHome_c [] targets, ClassQueryInterface_c test, boolean loadComponent)
{
    MooreStateMachine_c ret_val = null;
    if (targets != null)
    {
        for (int i = 0; i < targets.length && ret_val == null; ++i )
        {
           if (test != null) {
             MooreStateMachine_c candidate_val = getOneSM_MOOREOnR511(targets[i],true);
             if ( candidate_val != null && test.evaluate(candidate_val) )
             {
               ret_val = candidate_val;
               break;
             }
           }
           else {
             ret_val = getOneSM_MOOREOnR511(targets[i], loadComponent);
             if (ret_val != null) {
               break;
             }
           }
        }
    }
    return ret_val;
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(MooreActionHome_c target)
{
	return getOneSM_MOOREOnR511(target, true);
}
public static MooreStateMachine_c getOneSM_MOOREOnR511(MooreActionHome_c target, boolean loadComponent)
{
  if (target != null) {
	if(loadComponent){
	     target.loadProxy();
	}
    return target.MooreStateMachine ;
  } else {
    return null;
  }
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c [] targets,
    ClassQueryInterface_c test)
{
	return getManySM_MOOREsOnR511(targets, test, true);
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c [] targets,
    ClassQueryInterface_c test, boolean loadComponent)
{
  if(targets == null || targets.length == 0 || targets[0] == null)
    return new MooreStateMachine_c[0];
  
  
  LinkedHashSet<MooreStateMachine_c> elementsSet = new LinkedHashSet<MooreStateMachine_c>();
  for (int i = 0; i < targets.length; i++) {
	if(loadComponent && targets[i] != null && targets[i].MooreStateMachine == null)
	  targets[i].loadProxy();
    MooreStateMachine_c associate = targets[i].MooreStateMachine;
    if (targets[i] != null && associate != null
    	&& (test == null || test.evaluate(associate))) {
        	if (elementsSet.add(associate)){ 
            }
    }
  }

  MooreStateMachine_c[] result = new MooreStateMachine_c[elementsSet.size()];
  elementsSet.toArray(result);
  return result;
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c [] targets)
{
  return getManySM_MOOREsOnR511(targets, null);
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c target,
    ClassQueryInterface_c test, boolean loadComponent)
{
  if (target != null) {
    MooreActionHome_c [] targetArray = new MooreActionHome_c[1];
    targetArray[0] = target;
    return getManySM_MOOREsOnR511(targetArray, test, loadComponent);
  } else {
    MooreStateMachine_c [] result = new MooreStateMachine_c [0] ;
    return result ;
  }
  
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c target,
    ClassQueryInterface_c test)
{
    return getManySM_MOOREsOnR511(target, null, true);
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c target)
{
    return getManySM_MOOREsOnR511(target, null, true);
}

public static MooreStateMachine_c [] getManySM_MOOREsOnR511(MooreActionHome_c target, boolean loadComponent)
{
    return getManySM_MOOREsOnR511(target, null, loadComponent);
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(StateMachineState_c target)
{
  return MooreStateMachine_c.getOneSM_MOOREOnR511(MooreActionHome_c.getOneSM_MOAHOnR511(target)) ;
}

public static MooreStateMachine_c getOneSM_MOOREOnR511(StateMachineState_c target,
    ClassQueryInterface_c test)
{
  return MooreStateMachine_c.getOneSM_MOOREOnR511(MooreActionHome_c.getManySM_MOAHsOnR511(target), test) ;
}



      
  public void batchRelate(ModelRoot modelRoot, boolean notifyChanges, boolean searchAllRoots)
  {
      batchRelate(modelRoot, false, notifyChanges, searchAllRoots);
  }
  
  public void batchRelate(ModelRoot modelRoot, boolean relateProxies, boolean notifyChanges, boolean searchAllRoots)
  {
        InstanceList instances=null;
        ModelRoot baseRoot = modelRoot;

      // R510
      StateMachine_c relInst38542 = (StateMachine_c) baseRoot.getInstanceList(StateMachine_c.class).get(new Object[] {m_sm_id});
            // if there was no local element, check for any global elements
            // failing that proceed to check other model roots
      		if (relInst38542 == null) {
      			relInst38542 = (StateMachine_c) Ooaofooa.getDefaultInstance().getInstanceList(StateMachine_c.class).get(new Object[] {m_sm_id});
      		}
			if (relInst38542 == null && searchAllRoots && !baseRoot.isCompareRoot()) {
				Ooaofooa[] roots = Ooaofooa.getInstances();
				for (int i = 0; i < roots.length; i++) {
				    if(roots[i].isCompareRoot()) {
				         // never use elements from any compare root
				         continue;
				    }
					relInst38542 = (StateMachine_c) roots[i].getInstanceList(StateMachine_c.class).get(new Object[] {m_sm_id});
					if (relInst38542 != null)
						break;
				}
			}
			//synchronized
      if ( relInst38542 != null )
      {
          if (relateProxies || !isProxy() || (inSameComponent(this, relInst38542) && !isProxy())) {
	      relInst38542.relateAcrossR510To(this, notifyChanges);
	  }
	  }
	          
	}
  public void batchUnrelate(boolean notifyChanges)
  {
		NonRootModelElement inst=null;
      // R510
      // SM_SM
		  inst=IsSupertypeStateMachine;
			unrelateAcrossR510From(IsSupertypeStateMachine, notifyChanges);
          if ( inst != null ) {
			   inst.removeRef();
	      }
	}
  public static void batchRelateAll(ModelRoot modelRoot, boolean notifyChanges, boolean searchAllRoots) {
		batchRelateAll(modelRoot, notifyChanges, searchAllRoots, false);
  }	  
  public static void batchRelateAll(ModelRoot modelRoot, boolean notifyChanges, boolean searchAllRoots, boolean relateProxies)
  {
	InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
    synchronized(instances) {
        Iterator<NonRootModelElement> cursor = instances.iterator() ;
    	while (cursor.hasNext())
	    {
            final MooreStateMachine_c inst = (MooreStateMachine_c)cursor.next() ;
	        inst.batchRelate(modelRoot, relateProxies, notifyChanges, searchAllRoots );
	    }
	}
  }

  public static void clearInstances(ModelRoot modelRoot)
  {
    InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
    synchronized(instances) {
       for(int i=instances.size()-1; i>=0; i--){
              ((NonRootModelElement)instances.get(i)).delete_unchecked();
       }
    
    }
  }

  public static MooreStateMachine_c MooreStateMachineInstance(ModelRoot modelRoot, ClassQueryInterface_c test, boolean loadComponent)
  {
  		MooreStateMachine_c result=findMooreStateMachineInstance(modelRoot,test,loadComponent);
  		if(result==null && loadComponent){
     List pmcs =  PersistenceManager.findAllComponents(modelRoot,MooreStateMachine_c.class);
		for (int i = 0; i < pmcs.size(); i++) {
			PersistableModelComponent component = (PersistableModelComponent) pmcs
				.get(i);
			if (!component.isLoaded()) {
				try {
					component.load(new NullProgressMonitor());
					 result=findMooreStateMachineInstance(modelRoot,test,loadComponent);
					 if(result!=null) return result;
				} catch (Exception e) {
					CorePlugin.logError("Error Loading component", e);
				}
			}
		}
	    }
	    if(result!=null && loadComponent){
    	    result.loadProxy();
	    }
	  return result;  
  }
private static MooreStateMachine_c findMooreStateMachineInstance(ModelRoot modelRoot, ClassQueryInterface_c test, boolean loadComponent)
{
	InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
		synchronized (instances) {
            for (int i = 0; i < instances.size(); ++i) {
				MooreStateMachine_c x = (MooreStateMachine_c) instances.get(i);
				if (test==null || test.evaluate(x)){
					if(x.ensureLoaded(loadComponent))
					return x;
			}
		}
		}
			return null;
}
  public static MooreStateMachine_c MooreStateMachineInstance(ModelRoot modelRoot, ClassQueryInterface_c test){
     return MooreStateMachineInstance(modelRoot,test,true);
  }
  
  public static MooreStateMachine_c MooreStateMachineInstance(ModelRoot modelRoot)
  {
	 return MooreStateMachineInstance(modelRoot,null,true);
  }

  public static MooreStateMachine_c [] MooreStateMachineInstances(ModelRoot modelRoot, ClassQueryInterface_c test, boolean loadComponent)
  {	
            if(loadComponent){
  			   PersistenceManager.ensureAllInstancesLoaded(modelRoot, MooreStateMachine_c.class);
  			}
		    InstanceList instances = modelRoot.getInstanceList(MooreStateMachine_c.class);
			Vector matches = new Vector();
			synchronized (instances) {
                for (int i = 0; i < instances.size(); ++i) {
					MooreStateMachine_c x = (MooreStateMachine_c) instances.get(i);
					if (test==null ||test.evaluate(x)){
						if(x.ensureLoaded(loadComponent))
						matches.add(x);
			    }
				}
			if (matches.size() > 0) {
				MooreStateMachine_c[] ret_set = new MooreStateMachine_c[matches.size()];
				matches.copyInto(ret_set);
				return ret_set;
			} else {
				return new MooreStateMachine_c[0];
			}		
		} 
  }
  public static MooreStateMachine_c [] MooreStateMachineInstances(ModelRoot modelRoot, ClassQueryInterface_c test){
    return  MooreStateMachineInstances(modelRoot,test,true);
  }
  public static MooreStateMachine_c [] MooreStateMachineInstances(ModelRoot modelRoot)
  {
	return MooreStateMachineInstances(modelRoot,null,true);
  }

  public boolean delete()
  {
    boolean result = super.delete();
	boolean delete_error = false;
	String errorMsg = "The following relationships were not torn down by the Moore State Machine.dispose call: ";
	MooreActionHome_c testR511Inst = MooreActionHome_c.getOneSM_MOAHOnR511(this, false);

	if ( testR511Inst != null )
	{
   	delete_error = true;	        
	errorMsg = errorMsg + "511 ";
	}
	StateMachine_c testR510Inst2 = StateMachine_c.getOneSM_SMOnR510(this, false);

	if ( testR510Inst2 != null )
	{
	delete_error = true;	        
	errorMsg = errorMsg + "510 ";
	}
	if(delete_error == true) {

		if(CorePlugin.getDefault().isDebugging()) {
			Ooaofooa.log.println(ILogger.DELETE, "Moore State Machine", errorMsg);
		}
		else {
			Exception e = new Exception();
            e.fillInStackTrace();
			CorePlugin.logError(errorMsg, e);
		}
	}
	return result;
  }

  // end declare instance pool

  // declare attribute accessors
  public boolean isUUID(String attributeName){
      if(attributeName.equals("sm_id")){
         return true;
      }
      return false;      
  }      
 public String getCompUniqueID(){
    UUID tempID=null;
    long longID=0L;
    StringBuffer result= new StringBuffer();
    
    tempID= getSm_id();
    
        if(IdAssigner.NULL_UUID.equals(tempID))
	      tempID=getSm_idCachedValue(); 
	      result.append(Long.toHexString(tempID.getMostSignificantBits()));
          result.append(Long.toHexString(tempID.getLeastSignificantBits()));
    return result.toString();
 }
  // declare attribute accessors
  public long getSm_idLongBased()
  {
    if ( IsSupertypeStateMachine != null )
    {
      return IsSupertypeStateMachine.getSm_idLongBased();
    }
    return 0;  
  }          
  public java.util.UUID getSm_id()
  {
    if ( IsSupertypeStateMachine != null )
    {
      return IsSupertypeStateMachine.getSm_id();
    }
    return IdAssigner.NULL_UUID;
  }

 public boolean hasSuperType(){
 	return  ( IsSupertypeStateMachine != null );
 
 }                  

  public java.util.UUID getSm_idCachedValue()
  {
    if ( !IdAssigner.NULL_UUID.equals(m_sm_id) )
      return m_sm_id;
    else
      return getSm_id();
  }
  
  public void setSm_id(java.util.UUID newValue)
  {
	if(newValue != null){
	    if(newValue.equals(m_sm_id)){
	        return;
	    }
	}else if(m_sm_id != null){
	    if(m_sm_id.equals(newValue)){
	        return;
	    }
	}else{
	    return;
	}
	AttributeChangeModelDelta change = new AttributeChangeModelDelta(Modeleventnotification_c.DELTA_ATTRIBUTE_CHANGE, this, "Sm_id", m_sm_id, newValue,true); 
   m_sm_id = IdAssigner.preprocessUUID(newValue);
    Ooaofooa.getDefaultInstance().fireModelElementAttributeChanged(change);
  }
  // end declare accessors
  public static void checkClassConsistency (ModelRoot modelRoot) {
    Ooaofooa.log.println(
	  ILogger.OPERATION,
	  "Moore State Machine", //$NON-NLS-1$
	  " Operation entered: Moore State Machine::checkClassConsistency"); //$NON-NLS-1$
     if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == false) {  //$NON-NLS-1$
       return;
     }
     MooreStateMachine_c [] objs = MooreStateMachine_c.MooreStateMachineInstances(modelRoot,null,false); 
     
     for ( int i = 0; i < objs.length; i++) {
       objs[i].checkConsistency();
     }  
  } 
  public boolean checkConsistency () {
    Ooaofooa.log.println(
	  ILogger.OPERATION,
	  "Moore State Machine", //$NON-NLS-1$
	  " Operation entered: Moore State Machine::checkConsistency");  //$NON-NLS-1$
    if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == false) {  //$NON-NLS-1$
      return true;
    }
	ModelRoot modelRoot = getModelRoot();
    boolean      retval = true;
    class MooreStateMachine_c_test38544_c implements ClassQueryInterface_c
    {
	  MooreStateMachine_c_test38544_c( java.util.UUID            p38545 ) {
	  m_p38545 = p38545;
	  }
	  private java.util.UUID             m_p38545; 
	  public boolean evaluate (Object candidate)
	  {
	      MooreStateMachine_c selected = (MooreStateMachine_c) candidate;
	      boolean retval = false;
	      retval = (selected.getSm_id().equals(m_p38545));
	      return retval;
	  }
    }

    MooreStateMachine_c [] objs38543 = 
    MooreStateMachine_c.MooreStateMachineInstances(modelRoot, new MooreStateMachine_c_test38544_c(getSm_id())) ;

    if ( (  (objs38543.length) == 0) )
    {

      if (CorePlugin.getDefault().isDebugging()){
          Ooaofooa.log.println(ILogger.CONSISTENCY, "Moore State Machine", //$NON-NLS-1$
           "Consistency: Object: Moore State Machine: Cardinality of an identifier is zero. " //$NON-NLS-1$
           + "Actual Value: " + Integer.toString( objs38543.length ) ); //$NON-NLS-1$
      }
      else {
          Exception e = new Exception();
          CorePlugin.logError("Consistency: Object: Moore State Machine: Cardinality of an identifier is zero. " //$NON-NLS-1$ 
          + "Actual Value: " //$NON-NLS-1$
          + Integer.toString( objs38543.length )  , e); 
      }
      retval = false;

    }

    if ( (  (objs38543.length) > 1) )
    {

      if (CorePlugin.getDefault().isDebugging()){
          Ooaofooa.log.println(ILogger.CONSISTENCY, "Moore State Machine", //$NON-NLS-1$
           "Consistency: Object: Moore State Machine: Cardinality of an identifier is greater than 1. " //$NON-NLS-1$
           + "Actual Value: " //$NON-NLS-1$ 
           + Integer.toString( objs38543.length )  + " SM_ID: " + "Not Printable" ); //$NON-NLS-1$
      }
      else {
          Exception e = new Exception();
          CorePlugin.logError("Consistency: Object: Moore State Machine: Cardinality of an identifier is greater than 1. " //$NON-NLS-1$ 
          + "Actual Value: " //$NON-NLS-1$
          + Integer.toString( objs38543.length )  + " SM_ID: " + "Not Printable" , e); //$NON-NLS-1$
      }
      retval = false;

    }

          // Moore State Machine is a subtype in association: rel.Numb = 510
          // The supertype class is: State Machine
    class StateMachine_c_test38549_c implements ClassQueryInterface_c
    {
	  StateMachine_c_test38549_c( java.util.UUID            p38550 ) {
	  m_p38550 = p38550;
	  }
	  private java.util.UUID             m_p38550; 
	  public boolean evaluate (Object candidate)
	  {
	      StateMachine_c selected = (StateMachine_c) candidate;
	      boolean retval = false;
	      retval = (selected.getSm_id().equals(m_p38550));
	      return retval;
	  }
    }

    StateMachine_c [] objs38548 = 
    StateMachine_c.StateMachineInstances(modelRoot, new StateMachine_c_test38549_c(getSm_id())) ;

    if ( (  (objs38548.length) != 1) )
    {

      if (CorePlugin.getDefault().isDebugging()){
          Ooaofooa.log.println(ILogger.CONSISTENCY, "Moore State Machine", //$NON-NLS-1$
           "Consistency: Object: Moore State Machine: Association: 510: Cardinality of a supertype is not equal to 1. " //$NON-NLS-1$
           + "Actual Value: " + Integer.toString( objs38548.length ) ); //$NON-NLS-1$
      }
      else {
          Exception e = new Exception();
          CorePlugin.logError("Consistency: Object: Moore State Machine: Association: 510: Cardinality of a supertype is not equal to 1. " //$NON-NLS-1$ 
          + "Actual Value: " //$NON-NLS-1$
          + Integer.toString( objs38548.length )  , e); 
      }
      retval = false;

    }

          // Associated One Side: rel.Numb = 511
            // Link Object: Moore Action Home
            // Other Side Class: State Machine State
    return retval;
  }


  // declare transform functions
  public  void Dispose()
  {
      Ooaofooa.log.println(ILogger.OPERATION, "Moore State Machine", " Operation entered: MooreStateMachine::Dispose") ; 
               final ModelRoot modelRoot = getModelRoot();
Action_c [] v_actions = Action_c.getManySM_ACTsOnR514(ActionHome_c.getManySM_AHsOnR513(MooreActionHome_c.getManySM_MOAHsOnR511(this)));


Action_c  v_action = null;
for ( int i36631 = 0; i36631 < v_actions.length; i36631++)
{
  v_action = v_actions[i36631] ;

if (v_action != null) {
v_action.Dispose() ;
}
else {
  Throwable t = new Throwable();
  t.fillInStackTrace();
  CorePlugin.logError("Attempted to call an operation on a null instance.", t);
}



}

StateMachine_c v_sm = StateMachine_c.getOneSM_SMOnR510(this);


if ( (  (v_sm != null)) )
{

this.unrelateAcrossR510From(v_sm);



}

if ( delete() ) {
    Ooaofooa.getDefaultInstance().fireModelElementDeleted(new BaseModelDelta(Modeleventnotification_c.DELTA_DELETE, this));
}



   } // End dispose

  // end transform functions


  public Object getAdapter(Class adapter) {
    Object superAdapter = super.getAdapter(adapter);
    if(superAdapter != null) {
    	return superAdapter;
    }
	  return null;
  }
} // end Moore State Machine
