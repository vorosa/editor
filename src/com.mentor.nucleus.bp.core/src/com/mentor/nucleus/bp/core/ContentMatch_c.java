package com.mentor.nucleus.bp.core;
//====================================================================
//
// File:      com.mentor.nucleus.bp.core.ContentMatch_c.java
//
// WARNING: Do not edit this generated file
// Generated by ../MC-Java/java.arc, $Revision: 1.111 $
//
// (c) Copyright 2005-2014 by Mentor Graphics Corp.  All rights reserved.
//
//====================================================================

// No special imports
import java.util.*;
import java.lang.reflect.*;
import org.eclipse.core.resources.IFile;
import org.eclipse.core.resources.IResource;
import org.eclipse.core.runtime.IAdaptable;
import org.eclipse.core.runtime.IPath;
import org.eclipse.core.runtime.Path;
import com.mentor.nucleus.bp.core.util.PersistenceUtil;
import org.eclipse.core.runtime.NullProgressMonitor;
import com.mentor.nucleus.bp.core.ui.marker.UmlProblem;
import com.mentor.nucleus.bp.core.common.*;
abstract class EV_CONTENT_MATCH extends genericEvent_c {
	public abstract int getEvtcode();
}

public class ContentMatch_c extends NonRootModelElement
		implements
			IAdaptable,
			Cloneable {
	// Public Constructors
	public ContentMatch_c(ModelRoot modelRoot, java.util.UUID p_m_id,
			int p_m_startposition, int p_m_matchlength) {
		super(modelRoot);
		m_startposition = p_m_startposition;
		m_matchlength = p_m_matchlength;
		//pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
		m_id = IdAssigner.preprocessUUID(p_m_id);

		Object[] key = {m_id};
		addInstanceToMap(key);
	}
	static public ContentMatch_c createProxy(ModelRoot modelRoot,
			java.util.UUID p_m_id, int p_m_startposition, int p_m_matchlength,
			String p_contentPath, IPath p_localPath) {
		ModelRoot resolvedModelRoot = ModelRoot.findModelRoot(modelRoot,
				p_contentPath, p_localPath);
		// if a model root was not resolved it is most likely
		// due to a missing file of the proxy, defualt back to
		// the original model root
		if (resolvedModelRoot != null)
			modelRoot = resolvedModelRoot;
		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);
		ContentMatch_c new_inst = null;
		synchronized (instances) {
			Object[] key = {p_m_id};
			new_inst = (ContentMatch_c) instances.get(key);
		}
		String contentPath = PersistenceUtil.resolveRelativePath(p_localPath,
				new Path(p_contentPath));
		if (modelRoot.isNewCompareRoot()) {
			// for comparisons we do not want to change
			// the content path
			contentPath = p_contentPath;
		}
		if (new_inst != null && !modelRoot.isCompareRoot()) {
			PersistableModelComponent pmc = new_inst.getPersistableComponent();
			if (pmc == null) {
				// dangling reference, redo this instance
				new_inst.batchUnrelate();
				new_inst.m_startposition = p_m_startposition;
				new_inst.m_matchlength = p_m_matchlength;
				//pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
				new_inst.m_id = IdAssigner.preprocessUUID(p_m_id);

			}
		}
		if (new_inst == null) {
			// there is no instance matching the id, create a proxy
			// if the resource doesn't exist then this will be a dangling reference
			new_inst = new ContentMatch_c(modelRoot, p_m_id, p_m_startposition,
					p_m_matchlength);
			new_inst.m_contentPath = contentPath;
		}
		return new_inst;
	}

	static public ContentMatch_c resolveInstance(ModelRoot modelRoot,
			java.util.UUID p_m_id, int p_m_startposition, int p_m_matchlength) {
		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);
		ContentMatch_c source = null;
		synchronized (instances) {
			Object[] key = {p_m_id};
			source = (ContentMatch_c) instances.get(key);
			if (source != null && !modelRoot.isCompareRoot()) {
				source.convertFromProxy();
				source.batchUnrelate();
				source.m_startposition = p_m_startposition;
				source.m_matchlength = p_m_matchlength;
				//pre-process the uuid so that we re-use null uuid instance rather then creating a new one.           
				source.m_id = IdAssigner.preprocessUUID(p_m_id);

				return source;
			}
		}
		// there is no instance matching the id
		ContentMatch_c new_inst = new ContentMatch_c(modelRoot, p_m_id,
				p_m_startposition, p_m_matchlength);
		return new_inst;
	}
	public ContentMatch_c(ModelRoot modelRoot) {
		super(modelRoot);
		m_id = IdAssigner.NULL_UUID;
		Object[] key = {m_id};
		addInstanceToMap(key);
	}

	public Object getInstanceKey() {
		Object[] key = {m_id};
		return key;
	}

	public boolean setInstanceKey(UUID p_newKey) {

		boolean changed = false;
		// round p1
		// round p2
		// round p3
		// round p4
		// round p5
		if (m_id != p_newKey) {

			m_id = p_newKey;
			changed = true;
		}
		return changed;
	}

	public boolean equals(Object elem) {
		if (!(elem instanceof ContentMatch_c)) {
			return false;
		}
		// check that the model-roots are the same
		if (((NonRootModelElement) elem).getModelRoot() != getModelRoot()) {
			return false;
		}

		return identityEquals(elem);
	}

	public boolean identityEquals(Object elem) {
		if (!(elem instanceof ContentMatch_c)) {
			return false;
		}

		ContentMatch_c me = (ContentMatch_c) elem;
		// don't allow an empty id-value to produce a false positive result;
		// in this case, use whether the two instances are actually the same 
		// one in memory, instead
		if ((IdAssigner.NULL_UUID.equals(getId()) || IdAssigner.NULL_UUID
				.equals(((ContentMatch_c) elem).getId())) && this != elem) {
			return false;
		}
		if (!getId().equals(((ContentMatch_c) elem).getId()))
			return false;
		return true;
	}

	public boolean cachedIdentityEquals(Object elem) {
		if (!(elem instanceof ContentMatch_c)) {
			return false;
		}

		ContentMatch_c me = (ContentMatch_c) elem;
		if (!getIdCachedValue().equals(
				((ContentMatch_c) elem).getIdCachedValue()))
			return false;
		return true;
	}

	// Attributes
	private int m_startposition;
	private int m_matchlength;
	private java.util.UUID m_id;

	// declare association references from this class

	// referring navigation

	Match_c IsSupertypeMatch;
	public void relateAcrossR9801To(Match_c target) {
		relateAcrossR9801To(target, true);
	}
	public void relateAcrossR9801To(Match_c target, boolean notifyChanges) {
		if (target == null)
			return;

		if (target == IsSupertypeMatch)
			return; // already related

		if (IsSupertypeMatch != target) {

			Object oldKey = getInstanceKey();

			if (IsSupertypeMatch != null) {

				IsSupertypeMatch.clearBackPointerR9801To(this);

				if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == true) { //$NON-NLS-1$
					Ooaofooa.log
							.println(
									ILogger.CONSISTENCY,
									"ContentMatch_c.relateAcrossR9801To(Match_c target)",
									"Relate performed across R9801 from Content Match to Match without unrelate of prior instance.");
				}
			}

			IsSupertypeMatch = target;
			m_id = target.getId();
			updateInstanceKey(oldKey, getInstanceKey());
			target.setBackPointerR9801To(this);
			target.addRef();
			if (notifyChanges) {
				RelationshipChangeModelDelta change = new RelationshipChangeModelDelta(
						Modeleventnotification_c.DELTA_ELEMENT_RELATED, this,
						target, "9801", "");
				Ooaofooa.getDefaultInstance().fireModelElementRelationChanged(
						change);
			}
		}
	}
	public void unrelateAcrossR9801From(Match_c target) {
		unrelateAcrossR9801From(target, true);
	}
	public void unrelateAcrossR9801From(Match_c target, boolean notifyChanges) {
		if (target == null)
			return;

		if (IsSupertypeMatch == null)
			return; // already unrelated

		if (target != IsSupertypeMatch) {
			Exception e = new Exception();
			e.fillInStackTrace();
			CorePlugin.logError(
					"Tried to unrelate from non-related instance across R9801",
					e);
			return;
		}

		if (target != null) {
			target.clearBackPointerR9801To(this);
		}

		if (IsSupertypeMatch != null) {

			m_id = IsSupertypeMatch.getId();
			IsSupertypeMatch = null;
			target.removeRef();
			if (notifyChanges) {
				RelationshipChangeModelDelta change = new RelationshipChangeModelDelta(
						Modeleventnotification_c.DELTA_ELEMENT_UNRELATED, this,
						target, "9801", "");
				Ooaofooa.getDefaultInstance().fireModelElementRelationChanged(
						change);
			}
		}
	}

	public static ContentMatch_c getOneSR_CMOnR9801(Match_c[] targets) {
		return getOneSR_CMOnR9801(targets, null);
	}

	public static ContentMatch_c getOneSR_CMOnR9801(Match_c[] targets,
			ClassQueryInterface_c test) {
		ContentMatch_c ret_val = null;
		if (targets != null) {
			for (int i = 0; i < targets.length && ret_val == null; ++i) {
				ret_val = getOneSR_CMOnR9801(targets[i], test);
			}
		}

		return ret_val;
	}

	public static ContentMatch_c getOneSR_CMOnR9801(Match_c target) {
		return getOneSR_CMOnR9801(target, null);
	}

	public static ContentMatch_c getOneSR_CMOnR9801(Match_c target,
			boolean loadComponent) {
		return getOneSR_CMOnR9801(target.getModelRoot(), target, null,
				loadComponent);
	}

	public static ContentMatch_c getOneSR_CMOnR9801(Match_c target,
			ClassQueryInterface_c test) {
		if (target != null) {
			return getOneSR_CMOnR9801(target.getModelRoot(), target, test);
		}
		return null;
	}

	public static ContentMatch_c getOneSR_CMOnR9801(ModelRoot modelRoot,
			Match_c target, ClassQueryInterface_c test) {
		return getOneSR_CMOnR9801(modelRoot, target, test, true);
	}

	public static ContentMatch_c getOneSR_CMOnR9801(ModelRoot modelRoot,
			Match_c target, ClassQueryInterface_c test, boolean loadComponent) {
		return find_getOneSR_CMOnR9801(modelRoot, target, test);
	}
	private static ContentMatch_c find_getOneSR_CMOnR9801(ModelRoot modelRoot,
			Match_c target, ClassQueryInterface_c test) {
		if (target != null) {
			ContentMatch_c source = (ContentMatch_c) target.backPointer_IsSubtypeContentMatchIsSubtype_R9801;
			if (source != null && (test == null || test.evaluate(source))) {
				return source;
			}
		}
		// not found
		return null;
	}

	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c[] targets) {
		return getManySR_CMsOnR9801(targets, null);
	}
	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c[] targets,
			boolean loadComponent) {
		return getManySR_CMsOnR9801(targets, null, loadComponent);
	}
	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c[] targets,
			ClassQueryInterface_c test) {
		return getManySR_CMsOnR9801(targets, test, true);
	}

	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c[] targets,
			ClassQueryInterface_c test, boolean loadComponent) {

		if (targets == null || targets.length == 0 || targets[0] == null)
			return new ContentMatch_c[0];

		ModelRoot modelRoot = targets[0].getModelRoot();

		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);

		Vector matches = new Vector();
		for (int i = 0; i < targets.length; i++) {
			ContentMatch_c source = (ContentMatch_c) targets[i].backPointer_IsSubtypeContentMatchIsSubtype_R9801;
			if (source != null && (test == null || test.evaluate(source))) {
				matches.add(source);
			}
		}
		if (matches.size() > 0) {
			ContentMatch_c[] ret_set = new ContentMatch_c[matches.size()];
			matches.copyInto(ret_set);
			return ret_set;
		} else {
			return new ContentMatch_c[0];
		}
	}

	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c target) {
		if (target != null) {
			Match_c[] targetArray = new Match_c[1];
			targetArray[0] = target;
			return getManySR_CMsOnR9801(targetArray);
		} else {
			ContentMatch_c[] result = new ContentMatch_c[0];
			return result;
		}
	}

	public static ContentMatch_c[] getManySR_CMsOnR9801(Match_c target,
			boolean loadComponent) {
		if (target != null) {
			Match_c[] targetArray = new Match_c[1];
			targetArray[0] = target;
			return getManySR_CMsOnR9801(targetArray, loadComponent);
		} else {
			ContentMatch_c[] result = new ContentMatch_c[0];
			return result;
		}
	}

	public void batchRelate(ModelRoot modelRoot, boolean notifyChanges,
			boolean searchAllRoots) {
		batchRelate(modelRoot, false, notifyChanges, searchAllRoots);
	}

	public void batchRelate(ModelRoot modelRoot, boolean relateProxies,
			boolean notifyChanges, boolean searchAllRoots) {
		InstanceList instances = null;
		ModelRoot baseRoot = modelRoot;

		// R9801
		Match_c relInst38968 = (Match_c) baseRoot
				.getInstanceList(Match_c.class).get(new Object[]{m_id});
		// if there was no local element, check for any global elements
		// failing that proceed to check other model roots
		if (relInst38968 == null) {
			relInst38968 = (Match_c) Ooaofooa.getDefaultInstance()
					.getInstanceList(Match_c.class).get(new Object[]{m_id});
		}
		if (relInst38968 == null && searchAllRoots && !baseRoot.isCompareRoot()) {
			Ooaofooa[] roots = Ooaofooa.getInstances();
			for (int i = 0; i < roots.length; i++) {
				if (roots[i].isCompareRoot()) {
					// never use elements from any compare root
					continue;
				}
				relInst38968 = (Match_c) roots[i]
						.getInstanceList(Match_c.class).get(new Object[]{m_id});
				if (relInst38968 != null)
					break;
			}
		}
		//synchronized
		if (relInst38968 != null) {
			if (relateProxies || !isProxy()
					|| (inSameComponent(this, relInst38968) && !isProxy())) {
				relInst38968.relateAcrossR9801To(this, notifyChanges);
			}
		}

	}
	public void batchUnrelate(boolean notifyChanges) {
		NonRootModelElement inst = null;
		// R9801
		// SR_M
		inst = IsSupertypeMatch;
		unrelateAcrossR9801From(IsSupertypeMatch, notifyChanges);
		if (inst != null) {
			inst.removeRef();
		}
	}
	public static void batchRelateAll(ModelRoot modelRoot,
			boolean notifyChanges, boolean searchAllRoots) {
		batchRelateAll(modelRoot, notifyChanges, searchAllRoots, false);
	}
	public static void batchRelateAll(ModelRoot modelRoot, boolean notifyChanges, boolean searchAllRoots, boolean relateProxies)
  {
	InstanceList instances = modelRoot.getInstanceList(ContentMatch_c.class);
    synchronized(instances) {
        Iterator<NonRootModelElement> cursor = instances.iterator() ;
    	while (cursor.hasNext())
	    {
            final ContentMatch_c inst = (ContentMatch_c)cursor.next() ;
	        inst.batchRelate(modelRoot, relateProxies, notifyChanges, searchAllRoots );
	    }
	}
  }
	public static void clearInstances(ModelRoot modelRoot) {
		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);
		synchronized (instances) {
			for (int i = instances.size() - 1; i >= 0; i--) {
				((NonRootModelElement) instances.get(i)).delete_unchecked();
			}

		}
	}

	public static ContentMatch_c ContentMatchInstance(ModelRoot modelRoot,
			ClassQueryInterface_c test, boolean loadComponent) {
		ContentMatch_c result = findContentMatchInstance(modelRoot, test,
				loadComponent);
		if (result == null && loadComponent) {
			List pmcs = PersistenceManager.findAllComponents(modelRoot,
					ContentMatch_c.class);
			for (int i = 0; i < pmcs.size(); i++) {
				PersistableModelComponent component = (PersistableModelComponent) pmcs
						.get(i);
				if (!component.isLoaded()) {
					try {
						component.load(new NullProgressMonitor());
						result = findContentMatchInstance(modelRoot, test,
								loadComponent);
						if (result != null)
							return result;
					} catch (Exception e) {
						CorePlugin.logError("Error Loading component", e);
					}
				}
			}
		}
		if (result != null && loadComponent) {
			result.loadProxy();
		}
		return result;
	}
	private static ContentMatch_c findContentMatchInstance(ModelRoot modelRoot,
			ClassQueryInterface_c test, boolean loadComponent) {
		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);
		synchronized (instances) {
			for (int i = 0; i < instances.size(); ++i) {
				ContentMatch_c x = (ContentMatch_c) instances.get(i);
				if (test == null || test.evaluate(x)) {
					if (x.ensureLoaded(loadComponent))
						return x;
				}
			}
		}
		return null;
	}
	public static ContentMatch_c ContentMatchInstance(ModelRoot modelRoot,
			ClassQueryInterface_c test) {
		return ContentMatchInstance(modelRoot, test, true);
	}

	public static ContentMatch_c ContentMatchInstance(ModelRoot modelRoot) {
		return ContentMatchInstance(modelRoot, null, true);
	}

	public static ContentMatch_c[] ContentMatchInstances(ModelRoot modelRoot,
			ClassQueryInterface_c test, boolean loadComponent) {
		if (loadComponent) {
			PersistenceManager.ensureAllInstancesLoaded(modelRoot,
					ContentMatch_c.class);
		}
		InstanceList instances = modelRoot
				.getInstanceList(ContentMatch_c.class);
		Vector matches = new Vector();
		synchronized (instances) {
			for (int i = 0; i < instances.size(); ++i) {
				ContentMatch_c x = (ContentMatch_c) instances.get(i);
				if (test == null || test.evaluate(x)) {
					if (x.ensureLoaded(loadComponent))
						matches.add(x);
				}
			}
			if (matches.size() > 0) {
				ContentMatch_c[] ret_set = new ContentMatch_c[matches.size()];
				matches.copyInto(ret_set);
				return ret_set;
			} else {
				return new ContentMatch_c[0];
			}
		}
	}
	public static ContentMatch_c[] ContentMatchInstances(ModelRoot modelRoot,
			ClassQueryInterface_c test) {
		return ContentMatchInstances(modelRoot, test, true);
	}
	public static ContentMatch_c[] ContentMatchInstances(ModelRoot modelRoot) {
		return ContentMatchInstances(modelRoot, null, true);
	}

	public boolean delete() {
		boolean result = super.delete();
		boolean delete_error = false;
		String errorMsg = "The following relationships were not torn down by the Content Match.dispose call: ";
		Match_c testR9801Inst2 = Match_c.getOneSR_MOnR9801(this, false);

		if (testR9801Inst2 != null) {
			delete_error = true;
			errorMsg = errorMsg + "9801 ";
		}
		if (delete_error == true) {

			if (CorePlugin.getDefault().isDebugging()) {
				Ooaofooa.log.println(ILogger.DELETE, "Content Match", errorMsg);
			} else {
				Exception e = new Exception();
				e.fillInStackTrace();
				CorePlugin.logError(errorMsg, e);
			}
		}
		return result;
	}

	// end declare instance pool

	// declare attribute accessors
	public boolean isUUID(String attributeName) {
		if (attributeName.equals("id")) {
			return true;
		}
		return false;
	}
	public String getCompUniqueID() {
		UUID tempID = null;
		long longID = 0L;
		StringBuffer result = new StringBuffer();

		tempID = getId();

		if (IdAssigner.NULL_UUID.equals(tempID))
			tempID = getIdCachedValue();
		result.append(Long.toHexString(tempID.getMostSignificantBits()));
		result.append(Long.toHexString(tempID.getLeastSignificantBits()));
		return result.toString();
	}
	// declare attribute accessors
	public int getStartposition() {
		return m_startposition;
	}

	public void setStartposition(int newValue) {
		if (m_startposition == newValue) {
			return;
		}
		AttributeChangeModelDelta change = new AttributeChangeModelDelta(
				Modeleventnotification_c.DELTA_ATTRIBUTE_CHANGE, this,
				"Startposition", new Integer(m_startposition), new Integer(
						newValue), true);

		m_startposition = newValue;
		Ooaofooa.getDefaultInstance().fireModelElementAttributeChanged(change);
	}
	public int getMatchlength() {
		return m_matchlength;
	}

	public void setMatchlength(int newValue) {
		if (m_matchlength == newValue) {
			return;
		}
		AttributeChangeModelDelta change = new AttributeChangeModelDelta(
				Modeleventnotification_c.DELTA_ATTRIBUTE_CHANGE, this,
				"Matchlength", new Integer(m_matchlength),
				new Integer(newValue), true);

		m_matchlength = newValue;
		Ooaofooa.getDefaultInstance().fireModelElementAttributeChanged(change);
	}
	public long getIdLongBased() {
		if (IsSupertypeMatch != null) {
			return IsSupertypeMatch.getIdLongBased();
		}
		return 0;
	}
	public java.util.UUID getId() {
		if (IsSupertypeMatch != null) {
			return IsSupertypeMatch.getId();
		}
		return IdAssigner.NULL_UUID;
	}

	public boolean hasSuperType() {
		return (IsSupertypeMatch != null);

	}

	public java.util.UUID getIdCachedValue() {
		if (!IdAssigner.NULL_UUID.equals(m_id))
			return m_id;
		else
			return getId();
	}

	public void setId(java.util.UUID newValue) {
		if (newValue != null) {
			if (newValue.equals(m_id)) {
				return;
			}
		} else if (m_id != null) {
			if (m_id.equals(newValue)) {
				return;
			}
		} else {
			return;
		}
		AttributeChangeModelDelta change = new AttributeChangeModelDelta(
				Modeleventnotification_c.DELTA_ATTRIBUTE_CHANGE, this, "Id",
				m_id, newValue, true);
		m_id = IdAssigner.preprocessUUID(newValue);
		Ooaofooa.getDefaultInstance().fireModelElementAttributeChanged(change);
	}
	// end declare accessors
	public static void checkClassConsistency(ModelRoot modelRoot) {
		Ooaofooa.log.println(ILogger.OPERATION, "Content Match", //$NON-NLS-1$
				" Operation entered: Content Match::checkClassConsistency"); //$NON-NLS-1$
		if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == false) { //$NON-NLS-1$
			return;
		}
		ContentMatch_c[] objs = ContentMatch_c.ContentMatchInstances(modelRoot,
				null, false);

		for (int i = 0; i < objs.length; i++) {
			objs[i].checkConsistency();
		}
	}
	public boolean checkConsistency() {
		Ooaofooa.log.println(ILogger.OPERATION, "Content Match", //$NON-NLS-1$
				" Operation entered: Content Match::checkConsistency"); //$NON-NLS-1$
		if (Boolean.valueOf(System.getenv("PTC_MCC_ENABLED")) == false) { //$NON-NLS-1$
			return true;
		}
		ModelRoot modelRoot = getModelRoot();
		boolean retval = true;
		class ContentMatch_c_test38970_c implements ClassQueryInterface_c {
			ContentMatch_c_test38970_c(java.util.UUID p38971) {
				m_p38971 = p38971;
			}
			private java.util.UUID m_p38971;
			public boolean evaluate(Object candidate) {
				ContentMatch_c selected = (ContentMatch_c) candidate;
				boolean retval = false;
				retval = (selected.getId().equals(m_p38971));
				return retval;
			}
		}

		ContentMatch_c[] objs38969 = ContentMatch_c.ContentMatchInstances(
				modelRoot, new ContentMatch_c_test38970_c(getId()));

		if (((objs38969.length) == 0)) {

			if (CorePlugin.getDefault().isDebugging()) {
				Ooaofooa.log
						.println(ILogger.CONSISTENCY,
								"Content Match", //$NON-NLS-1$
								"Consistency: Object: Content Match: Cardinality of an identifier is zero. " //$NON-NLS-1$
										+ "Actual Value: " + Integer.toString(objs38969.length)); //$NON-NLS-1$
			} else {
				Exception e = new Exception();
				CorePlugin.logError(
						"Consistency: Object: Content Match: Cardinality of an identifier is zero. " //$NON-NLS-1$ 
								+ "Actual Value: " //$NON-NLS-1$
								+ Integer.toString(objs38969.length), e);
			}
			retval = false;

		}

		if (((objs38969.length) > 1)) {

			if (CorePlugin.getDefault().isDebugging()) {
				Ooaofooa.log
						.println(ILogger.CONSISTENCY,
								"Content Match", //$NON-NLS-1$
								"Consistency: Object: Content Match: Cardinality of an identifier is greater than 1. " //$NON-NLS-1$
										+ "Actual Value: " //$NON-NLS-1$ 
										+ Integer.toString(objs38969.length)
										+ " Id: " + "Not Printable"); //$NON-NLS-1$
			} else {
				Exception e = new Exception();
				CorePlugin
						.logError(
								"Consistency: Object: Content Match: Cardinality of an identifier is greater than 1. " //$NON-NLS-1$ 
										+ "Actual Value: " //$NON-NLS-1$
										+ Integer.toString(objs38969.length)
										+ " Id: " + "Not Printable", e); //$NON-NLS-1$
			}
			retval = false;

		}

		// Content Match is a subtype in association: rel.Numb = 9801
		// The supertype class is: Match
		class Match_c_test38975_c implements ClassQueryInterface_c {
			Match_c_test38975_c(java.util.UUID p38976) {
				m_p38976 = p38976;
			}
			private java.util.UUID m_p38976;
			public boolean evaluate(Object candidate) {
				Match_c selected = (Match_c) candidate;
				boolean retval = false;
				retval = (selected.getId().equals(m_p38976));
				return retval;
			}
		}

		Match_c[] objs38974 = Match_c.MatchInstances(modelRoot,
				new Match_c_test38975_c(getId()));

		if (((objs38974.length) != 1)) {

			if (CorePlugin.getDefault().isDebugging()) {
				Ooaofooa.log
						.println(ILogger.CONSISTENCY,
								"Content Match", //$NON-NLS-1$
								"Consistency: Object: Content Match: Association: 9801: Cardinality of a supertype is not equal to 1. " //$NON-NLS-1$
										+ "Actual Value: " + Integer.toString(objs38974.length)); //$NON-NLS-1$
			} else {
				Exception e = new Exception();
				CorePlugin
						.logError(
								"Consistency: Object: Content Match: Association: 9801: Cardinality of a supertype is not equal to 1. " //$NON-NLS-1$ 
										+ "Actual Value: " //$NON-NLS-1$
										+ Integer.toString(objs38974.length), e);
			}
			retval = false;

		}

		return retval;
	}

	public Object getAdapter(Class adapter) {
		Object superAdapter = super.getAdapter(adapter);
		if (superAdapter != null) {
			return superAdapter;
		}
		return null;
	}
} // end Content Match
