package com.mentor.nucleus.bp.core.ui;
//====================================================================
//
// File:    SpecializedPackageClassFormalizeOnSQ_CPWizardPage1.java
//
// WARNING: Do not edit this generated file
// Generated by arc/page.inc
//
// (c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.
//
//====================================================================
import org.eclipse.jface.viewers.IStructuredSelection;
import org.eclipse.jface.viewers.StructuredSelection;
import org.eclipse.jface.wizard.IWizardPage;
import org.eclipse.swt.SWT;
import org.eclipse.swt.events.ModifyEvent;
import org.eclipse.swt.events.ModifyListener;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.layout.GridLayout;
import org.eclipse.swt.widgets.Combo;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Event;
import org.eclipse.swt.widgets.Label;
import org.eclipse.swt.widgets.Listener;
import org.eclipse.swt.widgets.Text;
import org.eclipse.ui.IWorkbench;

import com.mentor.nucleus.bp.core.*;
import com.mentor.nucleus.bp.core.common.ClassQueryInterface_c;

import java.util.ArrayList;
import java.util.List;

public class SpecializedPackageClassFormalizeOnSQ_CPWizardPage1
		extends
			PtWizardPage implements Listener {
	public static final String copyright = "(c) Copyright 2003-2014 Mentor Graphics Corporation  All rights reserved.";

	IWorkbench workbench;
	IStructuredSelection selection;

	// widgets on this page (public for unit tests)
	public Combo SubsystemCombo;
	public Label SubsystemLabel;

	// cache for user choices
	Subsystem_c Subsystem = null;
	Subsystem_c[] SubsystemInstances;

	/**
	 * Constructors for SpecializedPackageClassFormalizeOnSQ_CPWizardPage1.
	 */
	public SpecializedPackageClassFormalizeOnSQ_CPWizardPage1() {
		super("");
		init();
	}

	public SpecializedPackageClassFormalizeOnSQ_CPWizardPage1(String name) {
		super(name);
		init();
	}

	private void init() {
		setTitle("Formalize");
		setDescription("Choose the class to import");
	}

	public void onPageEntry() {
		Ooaofooa modelRoot = Selection
				.getModelRoot((StructuredSelection) ((SpecializedPackageClassFormalizeOnSQ_CPWizard) getWizard())
						.getSelection());

		// cache for previous user selections
		ModelClass_c v_Class = ((SpecializedPackageClassFormalizeOnSQ_CPWizard) getWizard()).v_Class;

		// cache for context
		ClassParticipant_c v_cp = ((SpecializedPackageClassFormalizeOnSQ_CPWizard) getWizard()).v_cp;

		ModelClass_c v_mclass = ModelClass_c.getOneO_OBJOnR939(v_cp);

		java.util.UUID v_existingId = Gd_c.Null_unique_id();

		if (((v_mclass != null))) {

			v_existingId = v_mclass.getObj_id();

		}

		InteractionParticipant_c v_participant = InteractionParticipant_c
				.getOneSQ_POnR930(v_cp);

		Sequence_c v_sequence = Sequence_c.getOneSQ_SOnR929(v_participant);

		Communication_c v_communication = Communication_c
				.getOneCOMM_COMMOnR1126(v_participant);

		String v_domName = "";

		if (((v_sequence != null))) {

			v_domName = v_sequence.Getpath(true);

		}

		else if (((v_communication != null))) {

			v_domName = v_communication.Getpath(true);

		}

		class Domain_test9789_c implements ClassQueryInterface_c {
			Domain_test9789_c(String p9790) {
				m_p9790 = p9790;
			}
			private String m_p9790;
			public boolean evaluate(Object candidate) {
				Domain_c selected = (Domain_c) candidate;
				return (selected.getName().equals(m_p9790));
			}
		}
		Domain_c v_domain = Domain_c.DomainInstance(modelRoot,
				new Domain_test9789_c(v_domName));

		// select related by where USER::selectOne
		Subsystem_c[] v_SubsystemInstances = Subsystem_c
				.getManyS_SSsOnR1(v_domain);
		Subsystem_c v_Subsystem = null;
		int SubsystemInstCount;
		int SubsystemResultCount = 0;
		for (SubsystemInstCount = 0; SubsystemInstCount < v_SubsystemInstances.length; SubsystemInstCount++) {
			Subsystem_c selected = v_SubsystemInstances[SubsystemInstCount];
			if (User_c.Selectone(selected.getSs_id())) {
				SubsystemResultCount++;
			}
		}
		this.SubsystemInstances = new Subsystem_c[SubsystemResultCount];
		SubsystemResultCount = 0;
		for (SubsystemInstCount = 0; SubsystemInstCount < v_SubsystemInstances.length; SubsystemInstCount++) {
			Subsystem_c selected = v_SubsystemInstances[SubsystemInstCount];
			if (User_c.Selectone(selected.getSs_id())) {
				this.SubsystemInstances[SubsystemResultCount] = selected;
				SubsystemResultCount++;
			}
		}
		SubsystemCombo.removeAll();
		for (SubsystemInstCount = 0; SubsystemInstCount < SubsystemResultCount; SubsystemInstCount++)
			SubsystemCombo
					.add(((Subsystem_c) this.SubsystemInstances[SubsystemInstCount])
							.getName());
		if (SubsystemResultCount == 1) {
			SubsystemCombo.select(0);
			updateSelectedSubsystem();
		}

		if (((v_Subsystem != null))) {

			if (((v_Class != null))) {

			}

		}

	}

	public void createControl(Composite parent) {
		// create the composite to hold the widgets   
		GridData gd = null;
		Composite composite = new Composite(parent, SWT.NULL);

		// create the desired layout for this wizard page
		GridLayout gl = new GridLayout();
		int ncol = 5;
		gl.numColumns = ncol;
		composite.setLayout(gl);

		SubsystemLabel = new Label(composite, SWT.NONE);
		SubsystemLabel.setText("Subsystem");
		SubsystemCombo = new Combo(composite, SWT.BORDER | SWT.READ_ONLY);
		SubsystemCombo.setLayoutData(new GridData(GridData.FILL_HORIZONTAL));

		// set the composite as the control for this page
		setControl(composite);
		onPageEntry(); // Initialize the ui widget contents
		addListeners();
	}

	private void addListeners() {
		SubsystemCombo.addListener(SWT.Selection, this);
	}

	// public for unit tests
	public void updateSelectedSubsystem() {
		this.Subsystem = SubsystemInstances[SubsystemCombo.getSelectionIndex()];
		((SpecializedPackageClassFormalizeOnSQ_CPWizard) getWizard()).v_Subsystem = this.Subsystem;
	}

	/**
	 * @see Listener#handleEvent(Event)
	 */
	public void handleEvent(Event event) {
		if (event.widget == SubsystemCombo) {
			updateSelectedSubsystem();
		}
		getWizard().getContainer().updateButtons();
	}

	public boolean isPageComplete() {
		boolean isPageComplete = true;
		if (Subsystem == null)
			isPageComplete = false;
		return isPageComplete;
	}

	public IWizardPage getNextPage() {
		((SpecializedPackageClassFormalizeOnSQ_CPWizard) getWizard()).v_Subsystem = Subsystem;
		PtWizardPage page = (PtWizardPage) getWizard().getNextPage(this);
		page.onPageEntry();
		return page;
	}

	/**
	 * @see IWizardPage#canFlipToNextPage()
	 */
	public boolean canFlipToNextPage() {
		if ((PtWizardPage) getWizard().getNextPage(this) == null)
			return false;
		if (Subsystem == null)
			return false;
		return true;
	}

}
